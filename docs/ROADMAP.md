# PoE2 AI Wiki — 로드맵 (Roadmap)

> **문서 상태**: v0.4 (2026-08-04) — **P5 Exit 달성 기록**(학습 루프 왕복 실증). v0.3 P4.5 Exit 기록 + §2-1 백로그 신설(잔여 항목의 착수 조건·계획·완료 기준). v0.2 (2026-07-31) P4.5 신설분, v0.1 (2026-07-29) 확정분 유지. BLUEPRINT §15-2 "재구축 로드맵/MVP"의 상세.
> **설계 근거**: "KB 먼저"(BLUEPRINT §7·§17) + "코드 먼저, 수집은 그 다음"(KB_INGEST KI-7) + 반프록시(AD-8) — 각 단계의 완료는 느낌이 아니라 **측정 가능한 Exit 기준**으로 판정한다.
> **변경 규칙**: 단계 순서·Exit 기준의 구조적 변경은 사용자 상호협의 후에만.
> **진행 상태**: ✅ **P5 Exit 달성 (2026-08-04, 사용자 승인)** — 학습 루프 왕복 1회 실증(피드백→큐레이션 게이트→승격→다음 생성 반영). **다음 = P5 잔여(RAG·능동 탐사) 또는 P6** → ✅ **P4.5 Exit 달성 (2026-08-02, 사용자 승인)** — 빌드 설계 루프 완성. 블라인드 재현 4회로 검증(규율 도입 후 3·4차가 실제 인게임 좌초 원인 2건을 모델 등급 무관하게 무개입 검출). 잔여 항목은 §2-1 백로그 → ✅ P0 완료 → ✅ **P1a 완료** (2026-07-29, 시드 35·vocab v1·스키마·store 4층 검증·index self-healing, pytest 13 ✓, CI Win+mac green) → P1b 실질 완료(①~⑤ 가동) → **P3 Exit 달성 = ★MVP (2026-07-30)**: 사용자 자연어 요청(경계 감개 저주 스태킹 블러드 메이지) → KB 조회 → 후보 3안 → 선택안 3티어 조립(적법성 RC4 전 항목 통과) → PoB 실측 기록 (저주 0→5개 DPS **2,584→16,858** 델타 실측 — 산출물은 `artifacts/`라 미보존이고, 수치는 여기 남긴다) → 공유 코드 전달 → **사용자 확인: '빌드 자체는 오류가 없습니다'**. 잔여(주얼 어댑터·트리/아이템 최적화)는 P4 범위 ← → **P2 Exit 달성 = M1 (2026-07-30)**: 사용자가 새 세션에서 '생명력 스태킹 노드' 자연어 질의 → pok MCP 도구로 KB 기반 답변 실증 (품질 승인). 후속 개선: FTS body 색인·토큰 AND·어센던시 실명 부착 — ①젬 + ②트리 + ③유니크 완료 → **KB 16,495 레코드**. 완전성 기준 8중 확장(⑥⑦⑧) 가동 + 그로 드러난 결함 4건 교정 완료. ④ 베이스+모드 풀 **완결** — 판정(185 승격·682 원장)·Desecrated 249·ko 이름(모드 2,004+ko효과 3,616·베이스 1,642·훼손 249)·GAME_DATA 승격 3,616·증거 체인 manifest(§5) 성립. crafting-rules 판 규칙 **확정**(접사 한도·화폐 동작 14종·카오스 고정 기법 인사이트, 사용자 판정). 제작 화폐 259종 수록(⑥에서 앞당김).
> - ① 젬 900 수록 (fetch 1,992 → KI-8 판정 → merge, GAME_DATA 796)
> - ② 트리 4,553 수록 (일괄 엔드포인트 2회 수집, 청크 분할 merge, 엣지 보존). 마스터리 368 제외 확정(구역 라벨/그래픽, 사람 판정) + 어센던시 선택 허브 5 수록(끊겼던 13노드 복구) + stats 마크업·개행 정규화 + 능력치 택1 노드 293 구조 승격(`attribute_choice`)
> - ③ 유니크 493 수록 (무기88·방어구219·기타138·재배48, PoB 대사 469, 변형은 현재 패치본만) + 성유 부여방법 884 노드 보강. 재배판 베이스타입 48건 승계·PoB 변형 베이스 6건 교정
> - ④ 모드 8,508 + 베이스 1,768 수록. 제작규칙 RC4 원천(그룹 배타·ilvl·스폰 가중치·DropChance) 확보. poe2db 카탈로그 2,677항목 교차: 보류 867 → 185 승격 + 682 원장 제외(unobtainable_mods, 부활 감지), Desecrated 249 신규(SUPPORTED_INFERENCE), poe2db:<pool> 획득 경로 3,616건
> - ⑤ 서술 트랙: poe2wiki 원문 35/35 수집(revid 증거) + 자체 재작성 35건 knowledge/wiki/ 수록(UNVERIFIED, 코드 게이트 통과). 발견: Acrobatics 키스톤은 0.3.0에서 게임 제거, 시드 서포트 5종은 개명·티어화(Inspiration→Efficiency I 등) — 시드 정리 후속 판단 대기
> - 시드 정리(2026-07-30 승인): Acrobatics 제거(0.3.0 게임 제거, 원장 기록) · 개명 4종 병합(inspiration→efficiency-i, martial-tempo→rapid-attacks-i, scattershot→multishot-i, glaciation→freeze — 수작업 관계 이전, 큐레이션 승격)
> - 사람 확인 대기: 젬 보류 58, 미매칭 시드 잔여 4(fire-infusion=젬 아닌 메커니즘화·pierce=티어드·chain·unleash — 처리 방향 판단 필요), PoB 전용 유니크 11
>
> **신규 PC 셋업**: `gh auth login` → clone → `python3.13 -m venv .venv && .venv/bin/pip install -e ".[dev]"` → `.venv/bin/pre-commit install` → 데이터 repo clone(`gh repo clone skerfolg/poe2-ai-wiki-data artifacts/ingest-raw`)

---

## 0. 전체 그림

```
P0 부트스트랩
 └─ P1 KB 기반+수집  (P1a 시드 KB 실증 → P1b ingest 구현·대량 수집)
     └─ P2 MCP 조회            ← M1: 첫 사용자 가시 가치 (위키 엔진)
         └─ P3 PoB 계산+빌드 생성   ← ★ MVP 선언 지점
             └─ P4 트리 최적화
                 └─ P4.5 빌드 설계 루프   (2026-07-31 신설, BLUEPRINT §10.0)
                     └─ P5 학습 루프
                         └─ P6 위키·챗봇 확장
```

- **MVP = P3 완료** (청사진의 1차 목표 "빌드 추천·최적화"가 처음 성립하는 지점). P2 완료는 중간 마일스톤 **M1**.
- **PoB 리스크 조기 해소**: P1의 ingest가 PoB 덤프(LuaJIT 실행)를 필수로 요구(소스이자 완전성 기준 ②) → 최대 기술 리스크(LuaJIT·OS별 셋업)의 절반이 P1에서 자동 검증됨. P3 진입 전 나머지 절반(계산 왕복)만 짧은 스파이크로 확인.

## 1. 단계 상세

| 단계 | 내용 | Exit 기준 (측정 가능) |
|---|---|---|
| **P0. 부트스트랩** (짧게) | `pyproject.toml`(패키지 `pok`)·common·pre-commit·CI 골격·**import-linter**(계층 위반=CI 실패) | `pip install -e .` 성공 + 빈 테스트 통과 + 의존 방향 위반 시 CI 실패 확인 |
| **P1a. 시드 KB 실증** | condition vocab v1 + `knowledge/schema/` 작성 → **손으로 소수 레코드**(스킬10·서포트10·패시브10) 작성 → store 로드·검증·인덱스 빌드·검색·관계 순회 전 구간 통과. 스키마 결함을 "30개 수정"으로 조기 발견 | 시드 30개가 스키마 검증+`ensure_index()` 3트리거+`search_kb` 왕복을 통과. 시드는 이후 `tests/` 픽스처로 영구 재사용 |
| **P1b. ingest 구현·대량 수집** | ingest CLI(fetch·parse·match·merge·report) + PoB 덤프 스크립트 → **저비용 에이전트로 수집 가동**(kb-ingest 스킬, KI-7) + 서술 재작성 병행(KI-2) | **완전성 8중 기준(KB_INGEST §4) 전부 통과한 첫 KB 커밋** + 증거 체인(manifest→데이터 repo) 성립 |
| **P2. MCP 조회** — M1 | FastMCP 서버 + `search_kb`/`get_entry`(D14 2단계) + 관계 그래프 순회 도구. **한국어 부분어 검색 개선 검토**(현 unicode61은 '불꽃'→'전기불꽃' 미매칭, 2026-07-29 실증 — FTS5 trigram/prefix 후보) | **Claude/Codex 대화에서 실제 KB 질의 응답** — 위키 엔진 탄생 |
| **P3. PoB 계산+빌드 생성** — ★MVP | (진입 스파이크: headless 계산 왕복 Win/mac 검증) → PoB 어댑터·daemon·codec·버전맵 → `assemble`/`compute_pob`/`check_item_legality` → build-generation 스킬(후보 2~3 → 사용자 선택 → 3티어 조립·검증, D24/D18) | 자연어 요청 → **PoB 코드 산출** + `validation.json`에 다차원 목적(RC3) 실측 기록 |
| **P4. 트리 최적화** | `evaluate_delta`·`connect_anchors`(Steiner)·`optimize_tree` + PoB 상주 프로세스 성능(D23). **현황(2026-07-31): 도구 상자 구현 완료 + 주얼 평가·전략 모듈 — **Exit 달성 (사용자 승인 2026-07-31)** — 도구 상자(그래프·델타 실측·optimize+가지치기·주얼 템플릿·전략 모듈 3종) 완성, 실증: 경계 감개 DPS 27,664→59,001**(판정=사용자) | 베이스라인 대비 **포인트마다 PoB 델타로 정당화**되는 개선 산출 |
| **P4.5. 빌드 설계 루프** *(2026-07-31 신설)* | BLUEPRINT §10.0(D25~D30)의 구현: ① 설계 문서 규격 가동([BUILD_DESIGN.md](BUILD_DESIGN.md), design.md 읽기/쓰기·파서) ② 제약 검사기 5종(포인트 예산·색상 장부·점유·자원·**지속 가능성 경계**, D27) ③ 목표 상태 objective(임계값·캡 사전식, D28) ④ `parse_pob` 앵커 수집(`artifacts/anchors/`, D30) ⑤ build-generation 스킬을 설계 루프 기준으로 개정 + 설계 규율 6건(요구-수급·가정 수치 등재·축별 점유 장부·실측 이상 반증·성립 질문·루프 출력물 원칙). 선행 ingest: 젬 코스트·점유 전수(527건)·어센던시 연결 | ✅ **Exit 달성 (사용자 승인 2026-08-02)** — v6 청사진을 설계 루프로 재현: 제약 전 항목 기계 검증 + 미검증 가설 목록 + PoB 실측. **검증 방식 = 블라인드 재현 4회**(정답 격리 → 무개입 실행 → 채점): 규칙 도입 전 1차는 실제 인게임 좌초 원인 2건을 놓쳤고, 규율 2세대 도입 후 3·4차가 **모델 등급 무관(4차 Sonnet 5 고정)하게 2건 모두 무개입 검출** — 문서에 없던 성립 조건을 KB 인사이트+경계 산수로 도출. 잔여 결함은 §2-1 백로그로 이관(흐름 결함 0건) |
| **P5. 학습 루프** | feedback→큐레이션→승격(promote) 파이프라인(`learning/feedback.py`·`learning/curation.py`·`artifacts/promote.py`) + 스킬이 `insights/`를 소비하는 반영 경로. **입력 형식 = 설계 문서(design.md)의 미검증 가설 목록**(D29). **잔여(2026-08-04 갱신)**: ✅ 인사이트 검색(RAG, `search_insights`/`get_insight`) · ✅ **3계층 사다리**(BLUEPRINT §15-3 확정 — `scope` 필드, `set_scope`·`promote_to_record`) · ✅ **능동 탐사 트랙**(요구-공급 시너지 스캔·가설 큐 — 관계 엣지가 대량 레코드에 0건이라 조건 어휘 33종 기반으로 대체 설계) · ✅ **레코드행 실행**(durable 4건 → `resource.life`·`mechanic.reservation`·`defence.runic-ward`(신설)·`mechanic.trigger`(신설)·유니크 2건·CoMD 관계) · ✅ **보존 정책**(§9-3 확정 — 자동 삭제 없음, 계보가 보호 기준) · **→ P6 이관**: 메타 수집 `find_meta_builds`(외부 소스 fetch 필요, `src/pok/live/` 미착수. 앵커 수집은 P4.5에서 `artifacts/anchors/`·`parse_pob`로 이미 성립 — RC5 대응은 그쪽이 담당) | ✅ **Exit 달성 (사용자 승인 2026-08-04)** — 왕복 1회 실증: 사용자의 CoMD 빌드 폐기 노트(인게임 확인) → 주장 18건 추출 → **큐레이션이 오류 1건 기각**(성찬식 "5당 2%" — v6 인게임값 66%와 모순) → 17건 승격(인사이트 5건) → **블라인드 재생성이 KB 조회로 차단을 스스로 검출**. 대조: 1차 시도는 딜·워드·정신력 수지를 전부 계산한 뒤 전제가 무너졌으나, 실증에선 **장비 계산 이전에 정지**(연쇄 6화살표 중 A5 차단·폐기 계보 역추적·대안 분기 제시). 부수 성과: 점유 검사기 축 무관 통합(정신력 정액 축 갭 해소) |
| **P6. 위키·챗봇 확장** | 시맨틱 검색(하이브리드, D13)·i18n 심화·가격 추산 스킬(D19)·챗봇 범위(§15-4) | P6 진입 시 정의 |

## 1-2. 반사실 학습 캠페인 (M1~M6) — 2026-08-13 신설

> **왜 별도 절인가**: P0~P6은 **기능**의 단계이고 이건 **누적**의 단계다. M3부터는
> 「완료」가 없다 — 표본이 늘고 제안이 판정될수록 관측이 계속 늘어난다. 그래서 Exit
> 기준이 아니라 **의존 관계와 재개 규약**으로 관리한다.
> **읽을 것**: 절차는 `skills/counterfactual-measure/AGENTS.md`(P0~P4·재개 규약).

### 무엇을 만드는가

래더 코퍼스는 **채택된 것**만 담는다(positive only). 「이 노드를 빼면 얼마나 나빠지나」를
재서 negative를 만들면, 채택률과 곱해 **「전원이 찍지만 빼도 손실 0.1%」= 메타 습관**이
드러난다. 그게 갈아탈 수 있는 예산이고 창의의 안전 지대다(BACKLOG #62 「무엇을 만들 수
있나」의 축).

### 무엇을 어디에 쌓나 (사용자 승인 2026-08-17)

**층마다 자리가 다르다** — 래더 코퍼스가 「원시 PoB 코드(데이터 repo) →
`UsageProfile`(정본)」로 갈라진 것과 같은 구조다.

| 층 | 무엇 | 어디 | 선례 |
|---|---|---|---|
| **1층 관측** | 빌드별 제거 델타(약 78,000행) | **데이터 repo** `counterfactual/<시즌>/` | 래더 PoB 코드 5,497벌 |
| **2층 집계** | 노드별 가치 + 신뢰도 | **`knowledge/` 정본 레코드** | `UsageProfile` 110종 |

⚠ **1층만 쌓으면 엔진이 그걸 읽을 경로가 없다**(사용자 지적). 래더 코퍼스는
`UsageProfile`이 있어 `search_kb`·`suggest_anchors`가 쓰는데 반사실엔 그 자리가 없다.
**M4에서 정본 레코드 타입을 신설한다** — 그래야 앵커를 「많이 찍혔다」가 아니라
**「빼면 아프다」**로 고를 수 있고(BACKLOG #77), 그것이 계속 누적되는 장기 기억이다.
⛔ 단 **1층 원시는 정본에 쓰지 않는다**(재생성 가능한 파생 · 정본 커밋을 로그로 덮는다).

### 층 ↔ 스킬 단계 매핑

번호 체계가 둘이라 혼동이 있었다(사용자 지적 2026-08-13). 대응은 이렇다:

| 학습 층 | 스킬 단계 | 누가 |
|---|---|---|
| — (전제: 계기 검사) | **P0 교정** | 스크립트 |
| **1층 관측** | **P1 계획 · P2 측정 · P3 체크포인트** | 스크립트(CPU) |
| **2층 일반화** | **P4 집계** | 스크립트 |
| **3층 제안** | 스킬 밖 | **LLM** ← 창의는 여기서만 나온다 |
| **4층 정본화** | 스킬 밖 | 큐레이션(기존 경로) |

⚠ 1~2층에 LLM을 넣지 않는 것은 **재현성** 때문이다. 지난주 값과 이번주 값을 합칠 수
있어야 누적이 성립하고, 그래야 중단·재개가 가능하다. **3층에는 LLM이 필수다** — 교체
방향은 경우의 수가 전수 불가라(아래 수치) 「무엇을 시도할 가치가 있나」를 좁혀 줄 사전
분포가 필요하고, 규칙으로는 그게 안 된다. 실증: 눈알 왕관과 래스피스 구체는 **각각 단독
델타 0인데 함께 넣으면 1.44배**다 — 국소 탐색은 둘 다 탈락시킨다.

### 마일스톤

| | 무엇 | 누가 | 의존 | 상태 (2026-08-16 갱신) |
|---|---|---|---|---|
| **M1** | 하네스(도구) — `engine/tree/counterfactual.py` | codex | — | **완료** (PR #75 머지) |
| **M2** | 표본 확대(수집) — 컨셉 축 확장 | Mac 세션 | **없음**(M1과 무관) | **완료** (PR #78·#79·#82) |
| **M3** | **1층 관측** = 측정 캠페인(P1~P3) | 스크립트 | M1 · #73 저장 규약(**승인됨**) | **완료** (2026-08-17, 2,689/2,689 · 67,395행 · 데이터 repo 푸시) |
| **M4** | **2층 일반화** = 집계(P4) → **정본 레코드** | 스크립트 | M3 **완료** · ⚠ BACKLOG #81 | **완료** (2026-08-19) — `NodeValue` 2,546종 · 관측 57,122행. ⛔ 정본 승격은 M3 재측정 후 |
| **M4.5** | **조건 층** = 메커니즘 그룹 | 스크립트 | M4 | ⏸ **보류** — 조건이 일반화 안 됨(BACKLOG #85). 담체 파생은 유효 |
| **M5** | **3층 제안** = 창의 | LLM/codex | M4 (M4.5는 보류) | 설계 메모만 있음(아래 절) |
| **M6** | **4층 정본화** | 큐레이션(존재) | M5 반복 확인 | — |

**M2 완료분**(2026-08-16): 30컨셉 300벌 → **112컨셉 5,497벌**, 프로파일 28 → **110종**.
축을 열었다(`keypassives` 24 · `skills` 30 · `items` 28, 전부 n=50). 곁들여 세 가지를
고쳤다 — 재수집이 같은 캐릭터를 두 벌로 세던 것(`sample.superseded`) · 키스톤 취득
경로를 트리로만 세던 것(`observed.anchor`, BACKLOG #75) · 필수 앵커 기준이 표본
크기에 매여 있던 것(`ci_low >= 80`, 22종 합계 37 → 109개).

**완료된 선행**: P0 교정(`engine/corpus_fidelity.py` · PoB 불필요) · 캠페인 스킬 · 스킬
등록 shim(`test_skills_registered.py`가 강제).

**M3 완료(2026-08-17)**: 2,689벌 전량 측정 · **67,395행**(유효 99%) · 데이터 repo
`counterfactual/0-5/` 푸시(27MB). `measure-plan`/`measure-status`에 P0의 `usable`과
`pob_commit`을 박아 재개 판정이 성립한다.

**M4 착수 전 확인할 것 — BACKLOG #81(타임리스 주얼)**. 1층 관측은 주얼을 꽂은 채
쟀으므로 **빌드별로는 옳다**. 문제는 **집계**다: 코퍼스의 **9.0%(106/1,175 복원분)**가
타임리스를 들고 있고, 그 반경 안 노드는 **다른 노드로 변환된 상태**에서 측정됐다.
같은 `node_id`로 묶어 평균 내면 「노드의 값」이 아니라 「어떤 빌드에서는 다른 노드였던
것」이 섞인다. **집계 스키마에 그 구분이 있어야 한다** — 없으면 정본에 오염이 들어가고,
그건 되돌리기 어렵다(BLUEPRINT 함정 ②).

**M4의 다음 한 걸음**: 집계 스키마 확정(구조 결정 — 철칙 1상 사용자 합의 필요).
①레코드 타입 이름·필드 ②신뢰도 표현(`ci_low` 선례) ③타임리스 오염 표시 방식.

### 착수 순서 (사용자 승인 2026-08-16)

캠페인과 #70을 어느 쪽부터 하느냐를 정했다. **근거는 하나다 — 캠페인 비용이 #70의
`compute_tree` 연결에 곱셈으로 걸려 있다.** `evaluate_removals`는 지금
`compute_build`만 부르는데(`counterfactual.py`), 그게 래더 빌드 기준 3~6초다.
연결을 나중에 하면 **느린 도구로 캠페인을 돌린 뒤 다시 돌려야 한다.**

| 순서 | 무엇 | 왜 이 자리인가 |
|---|---|---|
| **1** | `compute_tree` 연결 (#70) — `deltas.py`와 **하네스 양쪽** | 캠페인·재검증 양쪽의 곱셈 인자. 실측 3.7배(환경 의존) |
| **2** | P0 재측정 + 아래 「제약 실측」 표 갱신 | 연결 **후**라야 「계산 1회」 행이 의미 있다 |
| **3** | #70 재검증(블러드 메이지) | 연결 전에는 시간 상한에 잘린다(이미 2회) |
| **4** | M3 착수 (P1~P3) | #73 판정 + 위 세 개가 준비 조건 |

**2의 결과 (2026-08-16 실측, 아래 표)**: 제거 축은 **전수가 들어왔다** — 2,689벌
전량 3.8시간이다(옛 계획은 46벌에 2시간이었다). 반면 **교체 축은 46일이라 여전히
전수 불가**다. 즉 **M5(3층 제안)의 존재 이유는 그대로다** — 경우의 수를 좁힐 사전
분포가 필요한 쪽은 교체이고, 그 근거 수치가 이제 「수십 시간」이 아니라 **46일**로
확정됐다. 우선순위 논거는 재검토 결과 **바뀌지 않는다.**

### 계획을 제약하는 실측 — **근거를 함께 적는다**

**전량 재측정 2026-08-16 (Mac)** — 아래 표는 이 실측이다. 세 축이 전부 바뀌었다:
표본이 12배 늘고(M2), 복원이 100%가 됐고(#77), 트리 전용 계산이 붙었다(#81+연결).

| 항목 | 값 | 근거(중요) |
|---|---|---|
| 원시 파일 / 고유 캐릭터 | **5,497 / 2,689** | 같은 캐릭터가 여러 컨셉 폴더에 겹친다. 복원 실패 **0** |
| 딜 비교 가능 | **2,689 / 2,689 (100%)** | #77 머지 후 `dropped_granted`가 **0건**이 됐다 (옛값 46/231) |
| 계산 1회 — 통째 로드 | **0.580초** (중앙, 0.354~0.711) | 래더 12벌 표본, 노드 130~158·스킬 6~13그룹 |
| 계산 1회 — **트리만** | **0.156초** (중앙, 0.130~0.246) | `compute_tree`. 배율 **3.7배**(빌드마다 2.4~5.5배) |
| 제거 후보 | 할당의 **21%** (중앙, 14~33%) | 빌드당 **29개** (19~43), 할당 중앙 136 |
| 제거 전량(2,689벌) | 약 **7.2시간** | ⚠ 빌드당 **9.6초 실측**(캠페인 전량 실행 2026-08-17) |
| 추가 후보(경로 ≤5) | **326개** (중앙, 182~502) | 미할당 목적지(노터블·키스톤·주얼소켓) |
| 교체 전량 | **46일 — 여전히 전수 불가** | 빌드당 `29 × 326 = 9,454`회(0.4시간) × 2,689벌 |

**무엇이 달라졌나**: 옛 계획은 「46벌에 2시간」이었다. 지금은 **2,689벌(58배)에
7.2시간**이다 — 하룻밤에 전수가 들어온다. 트리 전용 계산 없이 같은 일을 하면
**13시간**이라, 연결을 먼저 한 것이 여기서 값을 한다.

⚠ **빌드당 9.6초는 산수(5.1초)보다 크다.** 차이의 출처는 셋이다: ①실제 후보 수가
중앙 29보다 많은 빌드가 흔하다(관측 32~56) ②빌드마다 복원(`spec_from_pob`)과 후보
열거가 붙는다 ③택1 노드를 빼는 변형은 `attribute_choices`도 바뀌어 **통째 로드로
떨어진다**. **산수로 잡지 말고 실측을 쓸 것.**

⚠ **배율을 10.8배로 잡지 말 것.** 데몬 독스트링의 3.15초 → 0.29초는 **다른 환경**
수치다. 이 Mac에서 같은 래더 빌드를 재면 통째 로드가 애초에 0.58초라 배율이 3.7배다
(BACKLOG #76 — 윈도우는 `compute_build`이 2.6배 느리다). **분모가 환경마다 다르므로
캠페인 계획은 그 PC에서 다시 재고 쓴다.**

⚠ **「데몬이면 계산당 0.1초」로 계획하지 말 것.** 그건 빈 스펙 기준이다 — 트리 전용
계산조차 래더 빌드에서는 0.156초다.

✅ **「`46`은 코드에 종속된 수치다」 — 해소됨(2026-08-16).** 그 병목(아이템 부여 스킬
그룹이 보조와 함께 빠지는 것)은 **PR #77로 머지**됐고, 재측정에서 `dropped_granted`가
**0건**이 되어 딜 비교 가능이 **2,689/2,689(100%)**가 됐다. 남은 교훈은 형태다 —
**이 표의 수치는 코드에 종속된다.** `restore.py`·데몬이 바뀌면 P0을 다시 돌릴 것.

⚠ **`faithful`은 여전히 0/2,689다** — 게이트로 쓰지 말 것. 형식의 정보 손실
(`stat_set_index` 가정)이라 **어떤 빌드도 통과할 수 없다**. 이번 재측정에서도
`needs_decision`이 2,606건으로 대부분이 그 사유다. 딜 축 판정은 `damage_comparable`을
쓴다.

### ⚠ 2026-08-17 실측으로 흔들린 전제 둘 — 아래 절을 읽기 전에

**① 「교체는 전수 불가」의 근거가 약해졌다.** 46일은 **후보마다 전체 재계산**을
가정한 값이다. PoB에는 전용 빠른 경로가 있었다(BACKLOG #78 — `getMiscCalculator` +
모드 캐시): 미할당 노드 **3,793개를 22초**에 훑는다. 상위 30만 정밀 측정하면 빌드당
약 50초라, 전량이 **46일 → 약 37시간**이다. **M5가 「좁혀 주는」 몫의 상당 부분을
측정이 대신할 수 있다**(AD-3 — 새 상수를 만들기 전에 측정이 정하게 할 수 없나부터).

**② 대각선을 품질의 잣대로 쓰면 안 된다.** #70·#77이 「래더 중앙값 대각선에
도달하는가」로 판정해 왔는데, A/B에서 **폭은 75% → 78%로 거의 그대로인데 DPS가
53% → 175%**가 됐다. 좁은 트리가 원본의 1.75배를 냈다 — 폭은 값의 대리 지표가
아니다. ⚠ 단 그 175%는 **EHP를 안 잰 수치**라 유리대포일 수 있고, 검증 전에는
이 전제를 확정하지 말 것.

### M4.5 조건 층 — 왜 생겼나 (2026-08-18 신설)

**원래 계획에 없었다.** M4를 만들다 산출물을 검증하면서 나왔으므로 계보를 남긴다 —
없으면 다음 세션이 「M4 하다 만 것」으로 읽는다.

**문제**: 노드 가치를 전 빌드로 뭉쳐 중앙값을 내면 **조건부 노드가 0으로 희석된다**.
권능 충전 노드는 권능을 안 쓰는 빌드에서 진짜 0이고, 쓰는 빌드에서 크다. 실측
2026-08-18(573벌): 「쓸모없어 보이던」 92종 중 **27종이 작동할 땐 30% 넘게** 아팠다
(Mind Over Matter 작동률 1.4% · 작동시 36.6% · Wildsurge Incantation 4.8% · 33.5%).
반대로 **55종은 어느 빌드에서도 안 움직였다** — 둘이 깨끗하게 갈린다.

⛔ **축을 더 재도 안 고쳐진다.** 처음엔 「기록 축이 3개뿐(`CombinedDPS`·`Life`·
`TotalEHP`)이라 0이 나온다」로 진단했는데, 사용자가 방향을 바꿨다 — 필요한 것은
더 많은 잣대가 아니라 **언제 쓰이는지**다. 그리고 작동률/작동시 손실은 **이미 잰
데이터에서 나온다**(재측정 불필요).

**구조 — 세 층을 가른다**(사용자 요구: 「새로운 게 발견될 때마다 업데이트되는 구조」):

| 층 | 무엇 | 어디 | 갱신 |
|---|---|---|---|
| 정의 | 이 메커니즘의 담체(젬) | KB 태그·효과 문구에서 **파생** | 새 젬이 KB에 들어오면 **자동 합류** |
| 예외 | 태그로 안 잡히는 것 | `knowledge/ingest/mechanism-groups.json` | **손질이 일어나는 유일한 자리** |
| 증거 | 어느 노드를 켜나 | 코퍼스에서 재계산 | 시즌마다 |

⛔ **담체 목록을 손으로 들지 않는다.** 실측: 코퍼스에서 판별력을 보인 것만 실었더니
`발동` 그룹이 젬 **2종**이었는데 KB 태그로 뽑으니 **106종**이었다(치명타 시 시전·
소환수 사망 시 시전이 빠져 있었다 — 사용자가 잡았다). 그룹의 **정의**와 **증거**를
섞은 탓이다.

**파생 결과 16그룹**: 발동 106 · 마나 55 · 하수인 49 · 격노 25 · 권능 충전 24 ·
저주 23 · 발현 21 · 변신 20 · 인내 충전 19 · 격분 충전 18 · 잔재 17 · 함성 17 ·
에너지 실드 16 · 토템 16 · 정신력 15 …

**결과 (2026-08-19)**: 작동률·작동시 손실은 **들어갔고 유효하다**(#84 완료).
**그룹 조건은 보류**다 — 컨셉 홀드아웃에서 재현 58.5% vs 기저 53.0%로 일반화되지
않았다(#85). 코퍼스가 컨셉별로 잘려 있어 그 안에서만 성립하는 상관이었다.
⛔ 재료는 **버리지 않았다**: 담체 파생(`mechanism_groups.py`)은 검증과 무관하게
유효하고, 판정 잣대(`scripts/holdout_group_conditions.py`)를 남겨 표본 설계를 고치면
**같은 잣대로 다시 잰다**.

**근거 위치**: `src/pok/engine/mechanism_groups.py` · `tests/unit/test_mechanism_groups.py`

### M5 제안 층 — 설계 메모 (착수 전 읽을 것, 2026-08-13)

M5는 **LLM이 루프에 들어오는 유일한 지점**이다. 그런데 넣는 자리를 틀리면 비용이
폭발하거나 정본이 오염된다. 대화에서 정리한 것을 남긴다.

**2단 루프 — LLM은 바깥에만 둔다.**

    바깥 (LLM · 비싸다 · 수십~수백 회)   집계를 읽고 「가설 + 이유 + 변경안 묶음」을 낸다
    안쪽 (결정적 · 싸다 · 수천 회)        묶음을 구체 변형으로 펼쳐 PoB로 전부 잰다

상각비: 제안 1건(수천 토큰) → 변형 **20~50건** 측정(수 분 CPU). 제안 100건이면 대략
30~50만 토큰에 측정 2,000~5,000건이다. **측정 한 건마다 LLM을 부르면 PoB가 공짜로 하는
일에 토큰을 지불한다** — "노드 12345를 빼봐라"는 `for` 문이 할 일이다.

**LLM이 필수인 이유는 경우의 수다.** 제거는 규칙으로 전수 열거되지만(연결성) 교체는
제거 × 추가 후보라 전수가 불가능하다. 무작위 표본은 사실상 전부 쓰레기를 낸다 — 그
공간을 「그럴듯한 영역」으로 좁혀 줄 사전분포가 필요하고, 규칙으로는 그게 안 된다.
실증: 눈알 왕관과 래스피스 구체는 **각각 단독 델타 0인데 함께 넣으면 1.44배**다.
국소 탐색은 둘 다 탈락시키고, 짝지어 볼 이유를 아는 것은 메커니즘 모델을 가진 쪽뿐이다.

**⛔ 함정 셋 — 대응 없이 착수하지 말 것.**

1. **창의적 제안자는 reward hacking을 악화시킨다.** LLM이 PoB 숫자를 최적화하면 점수는
   높고 인게임에서 못 만드는 구성을 **반드시** 찾아낸다(철칙 4. 실측: 검사기 5종을
   통과한 빌드가 출혈 강도를 2.76배 부풀렸다). → 게이트가 먼저다. 순서는
   **게이트(존재) → 보상 채널(M1~M4) → 창의적 제안자(M5)**.
2. **제안이 「측정된 것」으로 굳는다.** LLM 가설이 정본에 사실처럼 들어가면 KB가
   오염된다(BACKLOG §3에 이관 보고 3건이 틀렸던 기록이 있다). → 데이터셋에서
   **제안(LLM 출처)과 측정(PoB 출처)을 같은 필드에 섞지 말 것.** 정본 진입은 기존
   경로(`learning/curation.decide` = 사람 판정)를 거친다.
3. **재현되지 않는다.** LLM 제안은 결정적이지 않다. **어느 모델·어느 프롬프트가 낸
   제안인지 스탬프**가 없으면 다시 돌릴 수도, 감사할 수도 없다(#58과 같은 형태).

⚠ 1~2층(관측·일반화)에 LLM을 넣지 않는 것은 **재현성** 때문이다. 지난주 값과 이번주
값을 합칠 수 있어야 누적이 성립하고, 그래야 중단·재개가 가능하다. 이 요구와
「LLM을 안쪽에 두지 않는다」는 같은 설계 결정이다.

### ⛔ 뒤집힌 전제 — 다시 파지 말 것

1. **`pruned_nodes`는 연결성 검출기가 아니다.** 「비연결 노드는 PoB가 잘라내고 `pruned`가
   잡는다」는 전제가 실측으로 반증됐다 — 래더 리치 복원본(127노드)에 시작점에서 닿지 않는
   3노드 군집이 있는데 PoB는 전부 할당하고 `pruned`를 비웠다(같은 3노드만 따로 요청하면
   셋 다 잘라낸다 = **문맥 의존**). 규모 1,916/16,725(11.5%)·75/116벌.
   → 연결성은 **KB 그래프**가 판정한다. KB 간선은 PoB 간선의 부분집합이므로(PoB 5,154 중
   KB 미보유 4·KB에만 있는 것 0) **보수적으로** 틀린다 — 안전한 방향이다.
   `pruned`는 한 방향만 유효: **서면 확실한 실패, 비면 증거 아님.**
   **하류 영향**: `engine/assemble.py`가 `is_tree_legal`만 보고 출고를 통과시킨다.
2. **계산 비용**(위 표) — 0.1초는 빈 스펙 기준이었다.

### 미해결 조정 항목 · 미결 판정

- **표본 기준 통일**: M1의 PR 본문은 「290벌 중 53벌」, P0 교정은 「231벌 중 46벌」이었다.
  대조 결과 290 = `_all` 폴더 제외 **파일 수**(중복 포함)이고 53도 중복 포함 계수다.
  **정답은 고유 캐릭터 기준** — 중복을 세면 같은 빌드를 두 번 측정한다.
  (2026-08-16 재측정으로 그 수치는 **5,497 파일 / 2,689 고유**가 됐다. 기준은 그대로다.)
- **번호 충돌**: M1이 백로그 항목 번호로 `#73`·`#74`를 썼는데 둘 다 이미 머지된 PR
  번호다. 백로그는 자기 번호를 독립 발급하지만 참조가 모호해진다. (2026-08-13 재확인:
  항목 #70·#71도 PR #70·#71과 이미 겹쳐 있어 **이 레포에서는 일상**이다 — 정리 불필요.)
- **미결 판정 — P4에 사후검증(교차검증)을 넣을 것인가** (제안 2026-08-13, 답 대기).
  표본을 나눠 40벌로 노드 가치를 뽑고 **나머지 6벌의 실측과 맞는지** 확인하는 것.
  「집계된 가치를 **다른 빌드에** 써도 되나」를 재는 유일한 방법이고, 그게 아니면 2층
  일반화는 자기 표본 안에서만 참일 수 있다. 지금 스킬 P4에는 **없다**.

### 저장 위치 (사용자 결정 2026-08-13)

계획·체크포인트·측정 결과는 전부 **데이터 repo**(`artifacts/ingest-raw` =
`poe2-ai-wiki-data`). Windows·Mac 양쪽이 이어받아야 하므로 gitignore인 `artifacts/`·`var/`는
쓸 수 없고, 측정은 재생성 가능한 파생이라 `knowledge/`(정본)도 아니다.

---

## 2-1. 백로그 — P4.5 검증에서 드러난 잔여 항목 (2026-08-02 이관)

> **성격**: 대부분 **흐름이 찾아낸 지식·파이프라인 공백**이다. 단 B-1 처리 중 **진짜 흐름 결함 1건**이 드러났다 — 도구의 미매칭(UNKNOWN)을 "KB 갭"으로 단정한 것. 도구(표기 후보 제시)와 규칙(조회 실패의 원인 규명 의무) 양쪽으로 교정 완료(2026-08-02). 각 항목은 착수 조건·방법·완료 기준을 함께 적는다.
>
> **B-7 추가·해결(2026-08-04)**: B-6이 파일 층만 지켜 같은 사고가 **층을 바꿔 재발**했다 (샤드 830건 → `_verification` 라벨 2건 → `promoted_to` 계보 1건). 원인은 하나 — "부분 갱신"을 "전체 교체"로 수행. 필드 층에 같은 원리 적용: 깊은 병합·필드 감소 거부·검사 선행. 재검증이 스키마만 보느라 값 소실을 통과시키던 구멍도 함께 메웠다.
>
> **B-8 추가·해결(2026-08-04)**: 같은 계약이 레코드에만 있어 **인사이트 front matter**가 무방비였다(쓰기 지점 3곳이 각자 파싱·재작성 — B-6 이전 구조 그대로). `kb/insights.patch_front_matter`로 단일화하고 store와 같은 계약(미지정 키 보존·None 삭제·소실 거부·원자적 쓰기)을 적용. `atomic_write`는 정본 쓰기의 공통 도구로 공개 — 레코드든 인사이트든 반토막은 똑같이 나쁘다.
>
> **B-9·B-10 추가(2026-08-04)**: 빌드 테스트 리포트에서 나온 **수집 갭**과 그에 의존하는 계산기. 둘 다 인사이트로 우회하면 안 되는 것들이다(KD-5) — 공개 데이터는 ingest가, 계산은 엔진이 맡는다. B-10은 B-9 선행.
>
> **진행**: B-1~B-4 **해결 완료(2026-08-02)**. B-4는 "인게임 필요"로 분류됐으나 실은 수집 데이터에 있었다 — 분류 자체가 오진이었고, 그 교훈이 §2-3 싼 경로 우선 소진 의무가 됐다. 잔여는 B-5(P5 착수 시)뿐.

| # | 항목 | 착수 조건 | 진행 계획 (방법) | 완료 기준 |
|---|---|---|---|---|
| B-1 | ~~갑옷 Spirit 접사 KB 갭~~ → ✅ **해결 2026-08-02: 갭 아님(조회 표기 오류)** | — | 원시 판정: 갑옷 플랫 정신력(`IncreasedSpirit1~8`, body_armour)·셉터 전용 %증가·메드베드 Soul 계열 모두 **게임에 실재하고 KB에도 수록됨**. UNKNOWN의 원인은 정본 `+N to Spirit`을 `+N to maximum Spirit`으로 조회한 표기 오류였다 | ✅ 도구: UNKNOWN 시 **표기 확인 후보 제시**(근접 후보 없으면 "실제 미수록일 수 있다"로 구분) + 규칙: **조회 실패의 원인 규명 의무**(BUILD_DESIGN §2-3) — 미매칭을 KB 갭으로 단정 금지 |
| B-2 | ~~보조 젬 색상 필드 미수록~~ → ✅ **해결 2026-08-02** | — | `reqStr/reqDex/reqInt` → `data.color` 수록(**525건**: red 187·blue 176·green 142·colorless 20). colorless는 별도 라벨 유지(분모 포함 방식 미검증, AD-3) | ✅ v6 §7.3 색상 장부 10건이 KB 조회만으로 **10/10 재현** — 문서 전사 의존 해소 |
| B-3 | ~~`pob_computable:false` 유니크 조립 불가~~ → ✅ **해결 2026-08-02** | — | `JewelSpec.allocates`(부여 노터블 node_id) → 트리 병합으로 **효과만** 재현. manifest `substitute_modeling`에 사유·방법과 **재현되지 않는 것**(소켓 소모·조달 가정) 명기 | ✅ 대체 조립 경로 가동 + 대체 사실이 계보에 기록되어 실측 해석 시 오독 방지 |
| B-4 | ~~신성 모독 점유·"룬 잔류물" 실명 (P5 인게임 필요로 분류)~~ → ✅ **해결 2026-08-02: 인게임 불요 — 수집 데이터에 이미 있었음** | — | ① 신성 모독 "저주당 60 점유"는 poe2db **버프 팝업** 블록의 서술형 표기(`Reserves N per X`)라 파서가 미스캔 → 전 `.Stats` 스캔 + 서술형 패턴 추가 ② "룬 잔류물" = **칼구르의 잔류물**(Spirit 30), KI-8 신호 A가 "From 카드 유무"만 봐서 실재 젬 54건을 보류시킨 것이 원인 → 신호 A 확장(레벨효과표) | ✅ 정신력 축 장부 미상 0건. 부수 성과: 실재 젬 54건 수록·merge 파괴 결함 2건 교정·**싼 경로 우선 소진 의무** 규칙화(BUILD_DESIGN §2-3) |
| B-5 | ~~v8 수동 대조군 처리~~ → ✅ **해결 2026-08-04 (P5 착수와 함께)** — `…-설계v8-수동대조군`은 채점 대조용으로만 보존하고 정본 계보에서 분리 유지. P5 실증 산출물은 루프 출력물(`20260804-시신걸음-0버튼-정신력스태킹`)이며 수동 편집 없음 | — | — | ✅ 정본 계보에 수동 편집본 미혼입 |
| B-6 | ~~KB 쓰기 단일 경로~~ → ✅ **해결 2026-08-03 (사용자 합의 후 구현, PR #11)** — store에 쓰기 API 3종(`write_shard`·`write_record`·`patch_records`) + 안전장치 3종(쓰기 후 재검증·근거 없는 감소 거부·원자적 쓰기), 9개 모듈 전수 이관, KB_DATA_MODEL §9-2에 계약 명문화. 전 파이프라인 재실행 후 정본 diff 0 | ~~통합 병합 + 합의~~ | 문제: KB_DATA_MODEL §9가 `store.py`를 **로드·검증**으로만 정의하는데 **쓰기 계약이 없어**, ingest 모듈 17개가 각자 `knowledge/`에 쓰고 KD-1 배치 규칙(개별 JSON/NDJSON 샤드)을 매번 재추론한다 → 한 곳만 빠뜨려도 샤드 전체가 파괴된다(2026-08-02 실측 830건 손실). 계획: ① `store`에 쓰기 진입점 신설, KD-1 배치 판단을 **한 곳에 캡슐화** ② ingest·후처리 모듈(merge·tree_merge·mods·gem_costs·gem_colors 등)을 그 경로로 이관 ③ KB_DATA_MODEL §9에 쓰기 계약 명문화 | `knowledge/` 직접 쓰기가 0건(전부 store 경유), 파괴 회귀 테스트가 **단일 지점**에서 커버, §9에 계약 기록 |
| B-9 | ~~ingest 갭: 출혈 스케일 규칙 · Monster Power 상수~~ → ✅ **해결 2026-08-05** | — | ① **상태이상 계열 9종 전량** 수록(PoB `Data.lua`의 `ailmentTypeList`·`defaultAilmentDamageTypes`·`buildupTypes`) — 출혈만 넣는 건 KD-5가 지적한 건 바이 건 실패라 계열째 받았다. 핵심은 `scales_from`(출혈=Physical·중독=Physical+Chaos·강한 기절=5속성) ② **Monster Power**는 poe2db 확정값(평균 1·강함 2~3·약함 0.5, Normal ×1·Magic ×2·Rare ×5, 유니크 고정 20) — 단 **등급 구분은 예상치**로 표기(범위 서술뿐이고 몬스터별 표는 비공개) ③ 진짜 갭은 따로 있었다: 메타 젬 **에너지 계수가 젬마다 다른데**(1·2·3·10·30) KB의 `description`이 요약본이라 빠져 있었다 — 원시 재파싱으로 해소 | ✅ `Mechanic` 5→15건 + 키워드 정의 248건. 세 값 모두 `search_kb`로 조회되고 소스 직접 판독 불필요 |
| B-10 | ~~메타 젬 발동률 계산기~~ → ✅ **해결 2026-08-05** | — | `engine/trigger.py` — 최종 Power(유니크는 배율 아닌 고정 20) → 타격당 에너지 → 최대 에너지(`10 x 시전시간/0.1s`) → 발동 간격. **THOR 공식은 절반만 맞았다**: 최대 에너지는 맞고 획득 `3 x Power`는 틀렸다(젬마다 1·2·3·10·30이고 3은 두 젬에만 해당). 그대로 믿었으면 나머지 젬이 전부 틀렸다. Power 기반이 아닌 젬(고정 25·이동거리·자원 등)은 **계산하지 않고 사유를 낸다** | ✅ `compute_trigger_rate` MCP 노출. 대상 Power 가정이 결과 `assumptions`에 실려 나가 예상치임이 드러난다 |
| B-11 | ~~메타 조회 부재: "KB에 무엇이 어떤 형태로 있나"~~ → ✅ **해결 2026-08-05** | — | `describe_kb`(전경)·`describe_type`(필드 충전율·값 분포)를 인덱스에서 산출(Skill 20ms·Passive 55ms, 정본 재파싱 없이). 한글 보유율은 필드 **이름**이 아니라 **내용**으로 잰다 — 명명 규약이 타입마다 달라 `_ko` 접미사만 세면 Passive가 19%로 보이지만 실제는 99.6%다. 덤: 수치 범위 조회 `find_by_value`(점유 검사기가 "정신력 40 남았다"까지 내고 멈추던 것) | ✅ Skill의 `category`가 12.5%만 채워져 있다는 사실이 1콜에 나온다. `search_kb` 0건에도 진단(`why`)이 붙어 파일 탐색 도피 경로가 닫혔다 |

> **병합 직후 처리 완료** (2026-08-03, 통합 세션이 PR #9로 반영): ① **모델 층** — `Record.in_shard` 채택(확장자 추론 대신 모델 판별자, AD-7). ② **삭제 정책** — 임시 수정이던 "미포함 무조건 보존"을 걷어내고 **원장 근거가 있는 제외 판정분만 삭제**(`removed_by_ruling`), 근거 없는 미포함분은 보존하고 `deletion_candidates`로 나열 — "지우기 전에 후보를 나열해 사람이 판단한다"(사용자 확립 원칙)의 구현. 같은 뿌리의 결함 3건(트리 성유 874건 삭제·유니크 사람 판정 되돌림·mods가 heart 샤드 삭제)도 함께 해소. **이 계약의 구조적 보증은 B-6(쓰기 단일 경로)이 잇는다.**

## 2. 파킹된 결정의 배치

| 결정 | 배치 |
|---|---|
| condition vocab 초판 범위 | **P1a 첫 작업** |
| build-id 체계 · artifacts(builds/sessions/feedback) 멀티 PC 동기화 확장 | P3 |
| 인사이트 3계층(canonical/durable/season) · 보존 정책 | P5 |
| **P4 Exit 판정** (도구 상자 결과 승인 여부) | **사용자 판정 대기** — 임의 기록 금지 |
| **P4.5 Exit 기준 확정** (v6 재현 제안의 승인·수정) | ✅ **사용자 확정 2026-08-02** — 기계 검증+가설 목록+PoB 실측까지, 인게임 사이클은 P5. **Exit 달성 승인 2026-08-02** |
| 위키/챗봇 범위(§15-4) · 가격 추산 구체화(§15-5) · i18n 수준(§15-6) | P6 진입 시 |

## 3. 운용 원칙

- **단계 건너뛰기 금지** — Exit 기준 미달 상태로 다음 단계 착수하지 않는다(반프록시: "얼추 된 것 같음"은 완료가 아님).
- **각 단계 완료 시** 해당 문서 갱신(PROJECT_STRUCTURE·KB_* 등) + BLUEPRINT 버전 갱신 검토.
- 병렬 여지: P1b 수집(저비용 에이전트)이 도는 동안 P2 MCP 서버 골격 개발 가능(수집은 감독만 필요하므로).

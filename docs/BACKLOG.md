# PoE2 AI Wiki — 결함·제안 백로그 (Backlog)

> **문서 상태**: v0.2 (2026-08-09) — 큐/이력 분리 · 번호 충돌 해소 · PR 번호 확정.
> v0.1 (2026-08-07) 신설.
> **목적**: **어느 세션이든 이 파일 하나로 이어받는다.** 결함 큐를 대화나 세션 간
> 메시지에만 두면 그 세션이 끝날 때 사라진다 — 실제로 이관 6차까지 매번 지시서를
> 손으로 다시 써야 했다. 규율을 문서에만 두면 안 지켜지듯(철칙 5), **상태를 세션
> 기억에만 두면 소멸한다.**
> **ROADMAP과의 차이**: ROADMAP은 **단계**(P0~P6)와 Exit 기준, 여기는 **큐**다.
> 단계 진행과 무관하게 들어오고 나간다.
> **변경 규칙**: 항목 추가·상태 갱신은 자유. **삭제는 해결·기각 사유를 남긴 뒤에만.**
>
> **번호 규칙 (v0.2에서 정함)**: 번호는 **이 파일이 발급한다.** `[빌드]` 세션의 보고
> 안 번호(#1~#8 등)와 섞이면 참조가 깨진다 — 실제로 `#17`이 두 항목에 붙어 있었고
> 나중 것을 **#29로 재발급**했다. 다음 발급 번호는 **#30**이다.
>
> **PR 번호를 「(이 PR)」로 적지 말 것** — 머지되면 무엇을 가리키는지 알 수 없다.
> 올린 뒤 실제 번호로 되돌아와 적는다(v0.2에서 17곳을 정정했다).

## 운용 — 누가 관리하고 어디로 보내나

**백로그 관리는 역할이지 특정 세션이 아니다.** 세션은 컨텍스트가 차면 끝나므로,
"그 세션에 보내라"로 고정하면 이 파일을 만든 이유(상태가 세션 기억에만 있으면
소멸한다)가 그대로 재발한다.

### 세션 구분 — 제목 접두

세션에는 원래 종류 구분이 없다(사용자가 만들면 그냥 세션이다). 그래서 **제목 접두로
기계적으로 찾을 수 있게** 한다.

| 접두 | 역할 |
|---|---|
| `[엔진]` | 엔진·KB 수정, **백로그 관리**, 작업 지시서 생성 |
| `[빌드]` | 빌드 생성·검증. 도구를 **쓰다가** 막힌 지점을 보고한다 |

찾는 법: `list_sessions` → `[엔진]` 접두 중 **가장 최근 활동**. 없거나 애매하면
**추측하지 말고 사용자에게 묻는다.**

⚠ 세션은 **자기 제목을 못 바꾼다**(`set_session_title`은 타 세션 전용). 사용자가
생성 시 붙이거나, 다른 세션이 붙여 준다.

### 역할 분리를 유지하는 이유

`[빌드]` 세션이 결함 12건을 찾아낸 것은 **도구를 만들지 않았기 때문**이다. 실제
빌드를 만들려다 벽에 부딪혀야 "조용히 빈 값이 온다"가 보인다. 도구를 만든 세션은
자기가 무엇을 넣었는지 알아서 그걸 못 본다. 실증: `radius-grant` 519건은 엔진
세션이 붙이고 "실규모"라 기록했는데, **오염된 한글에 매칭된 결과라는 걸 빌드
세션이 찾았다.** 두 역할을 섞으면 이 교차 검증이 사라진다.

### 접수는 2단계 — 즉시 등재, 검증은 착수 시

검증이 이 역할의 값어치지만(이관 3건이 틀렸다) **컨텍스트를 많이 쓴다.** 접수마다
재현하면 관리 세션이 금방 죽는다.

1. **접수 시** — 즉시 등재하되 `검증: 미검증(보고 원문)`으로 **명시**한다. 싸고,
   아무것도 잃지 않고, 거짓 확신도 안 생긴다.
2. **착수 시** — 실제로 작업할 때 재현·대조하고 `검증:` 칸을 채운다. 보고가 틀린
   것은 이때 드러난다 — 어차피 코드를 봐야 하니 비용이 중복되지 않는다.

### 인계

관리 세션의 컨텍스트가 차면 **이 파일에 상태를 반영하고** 끝낸다. 다음 `[엔진]`
세션이 이 파일을 읽고 그대로 이어받는다. 인계 메모를 따로 쓰지 않는다 — 그게
또 세션 밖에 있어야 할 상태를 세션 안에 두는 짓이다.

### 현재 열려 있는 것 (2026-08-09)

⚠ **이 표는 이력이 아니라 큐다.** 해결된 항목은 여기서 빼고 §4 표로 보낸다 — 그러지
않으면 표가 길어지기만 하고 "지금 뭐가 남았나"를 못 보여준다(실측: 해결분이 6줄 쌓여
그렇게 됐다).

| 항목 | 상태 | 다음 한 걸음 |
|---|---|---|
| #28 장갑 화염 저항 조용한 0 | 미검증 | 적법 베이스로 재현 — 안 되면 #27로 흡수 |
| **#32** KB에 베이스 implicit이 통째로 없음 | 검증됨(전수) | ingest에 `Data/Bases/*.lua`의 implicit 수록 — **동명 변종 전부** |
| #19 Mage's Legacy 개별 보너스 | 검증됨 | PoB `CalcPerform.lua:66~` 파싱해 수집 |
| #20 「피의 제물」 조건 미검사 | 미검증 | 같은 꼴이 몇 건인지부터 |
| #23 `optimize_rare` 합법성 위반 조립 | 미검증 | 조립 그리디가 한도·group을 지키게 |
| #22 퀄리티 축 | **부분 해결**(PR #53) | **임플리싯 축**은 남음 — 별도 설계 |
| #21 KB 조회 차단 | **부분 해결**(PR #49) | 부분 로드 여부 **판정 대기** |
| #25 이동속도 축 | **부분 해결**(PR #50·#51) | 가치 정량화 **판정 대기** |
| #16 PoB 핀 분산 | **완화됨**(PR #47·#48) | 근본(파생화)은 3곳까지 줄임, 그 이상은 불가 |
| #3 원소 집정관 | **부분 해결** | 남은 건 커스텀 주입 규약 확장 |
| #17 「Socketed Gems…」 7건 PoB 예외 | 미착수 | **판정 대기** — 0.5 실재 접사인가 PoE1 잔재인가 |
| #2 `liquid-emotion` 의미 오독 | 판정대기 | 게임 지식 |
| #4 부여 스킬 구분 · #5 `cost` 0/미수록 · #8-c 엔진 문구 | 미착수 | |

**기능 제안도 큐다** — 결함만 적으면 제안이 안 보인다(실측: v0.2 정리에서 제안 5건이
표에서 통째로 빠져 있었고 사용자가 지적했다):

| 제안 | 상태 | 다음 한 걸음 |
|---|---|---|
| **E — 조립 정본 루프 (스킬 신설)** · 사용자 발의 | 미착수 · **#26이 전제** | 순서 주의: **#27 도구 수정이 1순위**, 스킬은 그 위에 얹는 절차(철칙 5) |
| **A — `optimize_tree` 밀집도·비연결 + `exclude_nodes`** | 미착수 · **권장 1순위** | 반경은 **실측 목록**에서 받을 것(실효 최대 2,520 · Variable은 도넛) |
| B — 반경 주얼 소켓 평가 | 미착수 | #12 해결됐으므로 선행 조건은 풀렸다 |
| C — 좌표 대량 조회 경로 | 미착수 | 제안 A(`find_clusters`)가 생기면 자연히 해소 |
| `unscored_axes` (10차 이관 발의) | 미착수 | 축 판정 기준의 **압축**이 설계의 핵심 |
| ~~D — PoB 파싱 갭 감사기~~ | ✅ 구현됨 | 트리 PR #46 · 아이템 PR #48 |

### 처리 순서 (2026-08-09 확정)

**순서는 의존성이 정한다.** 막힌 것 → 그것이 막고 있던 것 → 값싼 것 → 큰 것.
이어받는 세션은 위에서부터 내려오면 된다.

| # | 항목 | 왜 이 자리인가 |
|---|---|---|
| 1 | **#26** 어센던시 조립 불가 | **차단성.** 이게 막혀 있으면 정본 경로(`assemble_pob`)가 통째로 안 돈다. 제안 E의 전제이기도 하다 |
| 2 | **#27 + #29** 적법성·요구속성 상시 부착 | **같은 수정 지점**(반환에 검사 결과를 싣는다)이라 함께. 제안 E의 1순위 |
| 3 | 4 | **#33** 최적화기 룬 소켓 미고려 | **이 세션 최대 축**(실측 DPS +69.6%). 후보가 작아 비용이 낮다. 3이 선행 |
| 5 | **#28** 장갑 저항 조용한 0 | 값싸다 — 적법 베이스로 재현만. 안 되면 #27로 흡수 |
| 6 | **제안 A 중 `exclude_nodes`만** | 순수 기능 추가라 즉시 가능. 배타 감지(본체)는 뒤로 |
| 7 | **제안 E** 조립 정본 루프(스킬) | 1·2가 끝나야 정식 경로가 선다 |
| 8 | **#19** Mage's Legacy 수집 | 검증 끝, PoB 파싱만 |
| 9 | **#23** `optimize_rare` 합법 조립 | 조립 그리디에 한도·group 반영 |
| 10 | **#20** 「피의 제물」 조건 | 같은 꼴 규모부터 세야 함 |
| 11 | **제안 A 본체** (`find_clusters`·비연결) | 크다. 반경 실측값이 이미 있어 설계는 섰다 |
| 12 | #22 임플리싯 축 · #16 근본 · #3 주입 확장 · 제안 B/C · `unscored_axes` | 나머지 |
| — | **#32** KB 베이스 implicit 수록 | ingest 계열이라 위 것들과 독립. 완드 16·목걸이 23종 전부 비어 있다 |
| — | #8-c · #4 · #5 | KB 수집·필드 설계 계열, 위 것들과 독립 |

⛔ **판정 대기는 순서에 넣지 않는다** — 내가 정할 수 없다. 답이 오면 그때 끼워 넣는다.

**판정 대기 5건** (사용자): #17 접사 성격 · #21 부분 로드 · #25 이동속도 가중치 ·
#2 필드 의미 · **제안 E의 스펙 파일 저장 위치**(철칙 1 — 구조 합의 사안).

**이관 접수 창구**: `[빌드]` 세션 → 살아 있는 `[엔진]` 세션 (상시).

**작업을 마치면 이 표와 해당 항목의 `상태:`를 갱신한다.** 안 하면 다음 세션이 이미
고친 것을 다시 파거나 낡은 정보로 판단한다 — 이 구조의 유일한 약점이다.
(⚠ 이것도 문서 규율이라 철칙 5에 따르면 안 지켜질 수 있다. 나중에 **PR에 백로그
변경이 없으면 경고**하는 식으로 기계화할 것.)

---

## 기재 규약

각 항목에 **반드시** 넣는다 — 없으면 다음 세션이 처음부터 다시 조사한다.

| 필드 | 왜 |
|---|---|
| **상태** | 미착수 / 조사중 / **원인규명** / 해결 / 기각 / **판정대기** |
| **실측** | 규모·수치를 **잰 값**으로. "많다"가 아니라 "1,536건" |
| **검증** | 무엇을 확인했고 **무엇을 가정했는가**. 이 구분이 오판을 막았다 |
| **근거 위치** | 파일·줄·레코드 id. 다음 세션이 바로 찾게 |

⚠ **보고를 그대로 옮기지 말 것.** 이관 3건이 실제로 틀렸다(아래 「검증으로 뒤집힌
보고」). 재현·대조 후에 적는다.

---

## 1. 결함 — 열린 것 먼저, 해결분은 아래에 기록으로

### #8-c 내부 엔진 문구가 효과 문구로 수록됨 (신규 접수, 2026-08-07)
- **상태**: 미착수 — #8-b 작업 중 드러난 **별개 결함**(범위를 넘겨 손대지 않음)
- **실측**: `monster no drops or experience [1]` · `use scaled contact offset [1]` ·
  `display statset no hit damage [1]` · `is resummoning minion [1]` 같은 **엔진 내부
  문구**가 `stats`에 그대로 들어 있다. `skill.wild-protector`는 분리 후 남은
  플레이어 `stats` **2줄이 전부 이 부류**다
- **왜 안 걸렸나**: `parse._INTERNAL_ID`는 **공백 없는** 식별자
  (`receive_bleeding_chance_%_when_hit`)만 걸러낸다. 띄어 쓴 내부 문구는 통과한다
- **주의**: 일괄 삭제 금지 — KB_INGEST 4-2의 판정(내부 stat id 32줄 **보존**)과
  충돌한다. 노드 고유의 조건·한계치일 수 있어 사람 판정이 선행이다
- **성격**: #8-b와 뿌리가 다르다. #8-b = *누구의* 줄인가, 이건 = *문구인가 내부값인가*

### #3 원소 집정관 — **PoB 미구현(계산 전량 0)** + 기저 수치 미수록 〔재분류〕
- **상태**: **부분 해결** (2026-08-08) — 할 일 1(표기)·4(경로 확인)는 제안 D 구현으로
  해결, **2(기저 수치 수집)·3(주입 규약)은 미착수**. 2026-08-07 재분류: 8차 이관
  (`[빌드]` 인퍼널리스트 자기번제 점화)이 "수집 누락"이 아니라 **PoB 계산 미구현 +
  수집 갭 복합**임을 보고, 엔진 세션이 기전까지 재현
- **원래 항목**: `mechanic.elemental-archon`에 지속시간·회복 기간이 없다(유지율 =
  지속 ÷ (지속+회복)인데 분자·분모가 둘 다 없다). 쿨다운 건과 동형 — 수정 모드
  (집정관 지속 15% 증가 등)는 수집되는데 **수정 대상 기저값이 없다**

#### 검증 (엔진 세션, 2026-08-07) — 결론은 보고대로, **기전은 보고보다 나쁘다**

**① PoB 계산 계층에 Archon이 없다 — 재현됨** (`external/pob/5d173cb`)

| 파일 | archon | onslaught(대조군) |
|---|---|---|
| `src/Modules/ModParser.lua` | **0** | 13 |
| `src/Modules/ConfigOptions.lua` | **0** | 4 |
| `src/Modules/CalcPerform.lua` | **0** | 12 |
| `src/Modules/CalcOffence.lua` | **0** | 0 |
| `src/Modules/CalcSetup.lua` | **0** | 1 |

⚠ `CalcOffence`는 대조군도 0이라 **그 파일만으로는 아무것도 못 말한다**.
근거가 되는 건 ModParser·ConfigOptions·CalcPerform 3개다.
데이터 계층엔 있다 — 저장소 전체 **611줄** 매치(0.5 트리만 113줄), 파일 26개.
`ConfigOptions`에 항목이 없으므로 **버프를 켤 방법 자체가 없다.**

**② 기전 — "패턴 없음"이 아니라 "부분 파싱 후 통째로 폐기"** (보고에 없던 것)

`src/Data/ModCache.lua`에 실제로 이렇게 들어 있다:
```lua
c["10% increased Archon Buff duration"]={{[1]={...name="Duration",type="INC",value=10}}," Archon Buff  "}
```
두 번째 반환값 `" Archon Buff  "`가 **미인식 잔여 텍스트**다. 즉 파서는
`increased ... Duration`만 잡고 **대상 한정어를 못 읽는다**. 그 결과:
- `src/Classes/PassiveSpec.lua:2586` `parseMod` → 잔여 있음
- `:2607-2611` `elseif unrecognizedMod then addition.extra = true`
- `:2625-2627` `if mod.list and not mod.extra then` → **적용 안 함**

**폐기되니 결과는 0으로 보고와 같다.** 하지만 이 기전이 중요한 이유는 두 가지다:
- 감사를 `grep archon`으로 하면 안 된다 — **`parseMod`의 잔여 텍스트를 봐야** 한다.
  그리고 그건 **도구화 가능**하다(→ 제안 D)
- 잔여 검사 없이 적용하는 경로가 어딘가 하나라도 있으면 「집정관 지속 30% 증가」가
  **범용 Duration 30% 증가**가 된다 — 점화 빌드에선 점화 지속이 늘어 **조용한 0이
  아니라 조용한 부풀림**이 된다. 트리(`PassiveSpec`) 경로는 막혀 있음을 확인했으나
  아이템·주얼 경로는 **미확인**

**③ `pob_computable` 선례 존재 — 확인됨.** `item.threaded-light.data`에
`pob_computable: false` + `pob_note: "PoB 미구현 — 커스텀 모드 수동 입력 필요"` +
`_verification.pob_computable: "IN_GAME"`. Passive 타입에도 이미 **13건(0.3%)** 있다.
같은 형식을 Archon 레코드에 붙이면 된다 — 새 필드 설계 불필요

#### 영향 — 조용한 0

집정관 관련 노드·룬·아이템·성유가 **전부 델타 0으로 측정되고 경고가 없다.**
보고가 열거한 것: 노드(눈보라/폭풍/불사/인내의 집정관 · 소생하는 집정관 · 집정관 효과
소형 6종 · 집정관 지연시간 · 힘 소진) · 어센던시(인퍼널리스트 「끓어오르는 육신」) ·
보조(「중재자의 점화」) · 성유(「지배」) · 아이템 접미(`of the Stars` · 아마나무 · 쿠르갈).
KB에 실재함은 확인(`search_kb("Archon")` → Item 2 · Mechanic 1 · Modifier · Passive 다수).

**이것이 `optimize_tree`가 집정관 노드를 하나도 안 뽑는 진짜 이유다.** 빌드 세션은
처음에 "후보 반경 밖"으로 오진했다 — 반경이 아니라 효과가 0으로 계산된 것이다.

⚠ **#13·제안 A와 교차한다.** 「힘 소진」(집정관 버프로 치명타 확률 30%)과 「지배」
성유의 상쇄는 제안 A가 `find_clusters`의 동기 사례로 든 조합인데, **지금 PoB에선 두
축 다 0으로 계산된다.** #13이 해금 배제를 고쳐도 이 노드의 값어치는 여전히 0으로
나온다 — 두 결함이 같은 노드에서 겹쳐 있다.

#### 실제 값어치 (보고 원문 — **미검증**, 엔진 세션이 재현하지 않음)

원소 집정관의 `25% more 원소 주문 피해`만 아이템 텍스트로 커스텀 주입:

| 구성 | 점화 확률 | IgniteDPS | 평균 피해 |
|---|---|---|---|
| 기준 (트리68 + 주얼5 + 장비크리 + 조프) | 27.53% | 852.9 | 28,819 |
| + 원소 집정관(25% more만) | 34.41% | **1,332.8 (+56%)** | 36,025 |

보고 주장: 나머지 절반(`주문 100% more 인화성 강도`)은 파싱 여부 불확실해 제외했으므로
**과소 추산**이다. 즉 PoB가 0으로 보는 축이 실제로는 최소 +56%.

#### 도구 감지 가능성 (철칙 5)

| 층 | 대상 | 상태 |
|---|---|---|
| **자동 보고** | KB 레코드 `pob_modeling` + `search_kb` 히트의 `pob_gap` 칸 | ✅ 2026-08-08 |
| **거부** | 미정 — 0 델타를 거부까지 할지는 오탐 위험(진짜 0인 노드도 있다) | ⬜ |
| **문서** | 커스텀 모드 주입 규약(대리 측정)·추산 표기 규칙 | ⬜ |

기저 수치(지속·회복) 자체는 **PoB에 없으므로 감지 대상이 아니다** — poe2db 직접
수집 경로가 필요하다.

#### 할 일
1. ✅ **트리 노드 표기** — 제안 D 구현(2026-08-08)이 집정관을 포함한 **500건**에
   `pob_modeling.kind: tree-line-unparsed`를 붙였다. ⚠ 원안의 `pob_computable: false`는
   **쓰지 않았다** — Passive에서 그 필드는 "노드가 PoB 트리에 아예 없다"는 다른 뜻이고
   (`tree_merge.py:88`), 겹치면 `items.py:284`류 후보 배제가 과잉 작동한다.
   ✅ **아이템 접미·룬도 표기됨** (2026-08-08, 1,864건 — 제안 D 아이템편)
2. ✅ **기저 수치 수록** (2026-08-08) — poe2db에 **있었다**. 출처 갭이 아니라 **수집 갭**이
   확정됐다(원인은 아래 #15). `mechanic.archon-buff`(신규)와 `mechanic.elemental-archon`
   양쪽에 `base_duration_s: 10` · `recovery_period_s: 20`. 자식 쪽엔 `base_values_from`으로
   출처를 남겼다 — 관계 어휘가 13종 closed enum이라(BLUEPRINT §7) `variant_of`를 새로
   만들 수 없어 값을 복사하되 계보를 밝히는 쪽을 택했다. **유지율 = 10/(10+20) = 33.3%**
3. ✅ **주입 규약** (2026-08-08) — 문서로만 두지 않고 **경로를 만들었다**(철칙 5):
   `ItemSpec.substitutes`에 적으면 manifest `substitute_modeling.injected_lines`에
   자동 기록된다. `text`에 섞으면 진짜 모드와 구분되지 않아 **추산이 실측으로 둔갑**하므로
   칸을 분리했다. 규약 본문은 `skills/build-generation/AGENTS.md` §대리 측정
4. ✅ 트리(`PassiveTree.lua:487`)·아이템(`Item.lua:2138`) **양쪽 다** 잔여를 검사함을
   확인 → 조용한 **0**이지 부풀림이 아니다
- **묶기**: `far_clusters`(제안 A) 작업과 같은 레코드를 만짐 → 함께 처리 권장

#### 회귀 테스트 (2026-08-08 구현 — `tests/integration/test_tree_parse_gaps.py`)
1. ✅ 갭 노드 할당 델타가 0인가 — **양방향**으로 건다: 집정관 지속시간(5695) **0개**,
   대조군 생명력의 차크라(35031) **57개**. 한쪽만 보면 "원래 효과 없는 노드"와 구분이
   안 된다
2. ✅ 경고가 뜨는가 — `search_kb` 히트의 `pob_gap` 칸 + 레코드의 `pob_modeling`
3. ✅ `mechanic.elemental-archon` 지속·회복 수치 수록됨

#### 미검증으로 남기는 것
- **+56% 커스텀 주입 실측** — 보고 원문. 엔진 세션이 재현하지 않았다
- ~~`주문 100% more 인화성 강도`를 PoB가 파싱하는지~~ → **파싱한다**(9차 이관 실측:
  아이템 텍스트 주입 시 점화 확률 66.12% → **72.68%**). 즉 앞선 +56%는 역시 과소 추산이었다
- ✅ **유지율 상한이 확정됐다** (9차 이관): 원문의 `You cannot gain any Archon Buff
  while you already have one` 때문에, 「지배」 성유(회복 기간 제거)가 없으면 **재획득
  트리거를 아무리 자주 걸어도 유지율이 33.3%에 묶인다**
- ~~지속 10초 / 회복 20초는 커뮤니티 2차 출처~~ → **poe2db로 확인됨**(2026-08-08).
  `/us/Archon_Buff`·`/kr/Archon_Buff` 양쪽 원문 대조: "By default, each Archon Buff lasts
  10 seconds" · 회복 기간 "lasts 20 seconds by default". **커뮤니티 값이 맞았다**
- 611줄 중 계산에 실제로 쓰이는 게 정말 하나도 없는지 전수 확인 안 함
  (Modules 5개 + PassiveSpec 적용 경로만 봤다)
- 영향 목록의 노드·아이템 열거는 보고 원문 — 개별 대조 안 함

### #16 〔신규〕 PoB 스냅샷 핀이 **8곳에 복사돼 있다**
- **상태**: **완화됨(테스트로 방어), 근본 미해결** (2026-08-08)
- **무엇**: `manifest.json`의 `pob_commit`이 정본 핀인데 같은 값이 CI 워크플로 2개와
  소스 상수·경로 5곳, 테스트 1곳에 **손으로 복사돼 있다**. 하나만 빠뜨리면 계산은 새
  스냅샷으로 돌고 카탈로그 검증은 옛 파일을 읽어 **증거 체인(AD-2)이 조용히 거짓**이 된다
- **근거 위치**: `.github/workflows/{ci,pob-smoke}.yml` · `pob/catalog.py:35` ·
  `kb/ingest/ailments.py:32` · `kb/ingest/merge.py:20` · `kb/ingest/__main__.py:192,348` ·
  `tests/unit/test_uniques.py:61`
- **조치(완화)**: `tests/unit/test_pob_pin_consistency.py`가 전 핀을 manifest와 대조하고
  **옛 커밋 잔존까지** 잡는다. 돌연변이로 감지력 확인(상수만·워크플로만 어긋나게 → 둘 다 실패)
- **남은 것**: 상수들이 `resolve_snapshot()`으로 **파생**되면 사이트가 8 → 3(manifest +
  워크플로 2)으로 준다. 소스 5곳은 애초에 중복이다. 다만 CI 워크플로는 파이썬을 못 읽어
  남는다
- **관련**: `skills/pob-snapshot/` — 교체 절차. 문서는 안 지켜지므로(철칙 5) 위 테스트가
  실제 방어선이다

### #17 〔신규〕 「Socketed Gems are Supported by …」 접사에서 **PoB가 예외를 던진다**
- **상태**: 미착수 — 아이템 파싱 갭 감사(제안 D) 중 발견, 범위를 넘겨 손대지 않음
- **실측**: **7건**. `modifier.controlleddestructionsupportunique-1` ·
  `…-1new-alt1` · `displaysupportedbycoldpenetrationunique-1` ·
  `displaysupportedbyknockbackuniqueglovesstr5` · `displaysupportedbymanaleechunique-1` ·
  `supportedbyinnervateunique-1` · `-2`
- **무엇**: 「Socketed Gems are Supported by Level 10 Controlled Destruction」류 문구를
  아이템에 얹으면 `new("Item", raw)`가 **죽는다**. PoB가 문구는 읽고 그 서포트 젬을
  찾다 실패하는 것으로 보인다 — 이름이 **PoE1 잔재**라 PoE2 데이터에 없다
- **왜 별개인가**: 다른 갭은 **조용한 0**(계산에서 빠짐)인데 이건 **예외**다. 실제 빌드에
  이 접사가 들어가면 조립 자체가 실패할 수 있다 — 성질도 영향도 다르므로 섞지 않았다
- **판정 필요**: 이 7건이 **0.5에 실재하는 접사인가** 아니면 수집이 긁어 온 PoE1 잔재인가.
  후자면 KB에서 빼는 게 맞고, 전자면 대체 표기가 필요하다 (게임 지식 판정)
- **근거 위치**: `pok.pob.item_parse_gaps`의 `parse_errors` 요약 칸

### 세 결함이 같은 형태다 — `unscored_axes` 제안 (10차 이관)

#18(방어 축) · #22(퀄리티 축) · #25(이동속도 축)는 전부 **"측정 축에 없어서 최적화가
구조적으로 못 보는 것"**이고, **셋 다 사용자가 지적해줘야 발견됐다.**

발의: `optimize_items`/`optimize_rare` 반환에 **`unscored_axes`** 칸을 두고, 후보가
건드리는데 `weights`에 없는 축을 나열한다. 채택은 호출자가 정하되 **안 보이는 채로
배제되는 일은 없어진다**(`axes`의 `empty` 보고와 같은 설계).

⚠ 착수 전 정할 것: "후보가 건드리는 축"을 무엇으로 판정하나. #18은 **미리 고른 방어
축 목록**으로 풀었는데, 일반화하면 델타가 0이 아닌 축 전부가 후보라 **619개 중 수십
개**가 매번 딸려 온다. 압축 기준(상대 변화율·화이트리스트)이 설계의 핵심이다

### #19 `mechanic.mages-legacy` **개별 보너스가 KB에 없다**
- **상태**: **검증됨** (엔진 세션 2026-08-08) — 수집 작업만 남음
- **보고**: 레코드에 **이름 14종만** 있고 각 보너스가 없어, 마법사의 피를 평가하려면
  PoB 소스를 직접 읽어야 했다
- **검증**: PoB에 전체 테이블이 **실재**한다 — `Modules/CalcPerform.lua:66`부터
  `local legacies = { LegacyOfAmethyst = { effects = {{stat="ChaosResist", type="BASE",
  value=45}} }, … }`. 보고의 값 형식과 일치
- **설계를 바꾸는 정보**: Ruby·Sapphire·Topaz가 **최대 저항 +5**를 준다. KB만 보면
  알 수 없다
- **중복 배수**: `globalEffect = 1 + 중복수 × (효과증가/100)`을 곱한 뒤 `m_floor`
  (`CalcPerform.lua:1502-1530`) — 보고 원문, 줄 번호는 미대조
- **성격**: 수집 갭. PoB 소스에서 파싱하면 되므로 poe2db 왕복 불필요

### #20 「피의 제물」 — PoB가 **조건을 검사하지 않는다**
- **상태**: 미검증(보고 원문) — 재현 안 함
- **보고 실측**: `LifeCost = 0`인 빌드(인퍼널리스트·혈액술 없음·래스피스 미착용)에서
  `passive.sacrificial-blood-14294`(생명력 소모 주문의 주문 피해 40% 증가)를 빼자
  **IgniteDPS −1,674.5**. 즉 조건 미충족인데 40%가 적용됐고 **최종 수치의 17.4%가 허수**
- **왜 위험한가**: 손으로 찾을 때까지 최적화기가 이 노드를 계속 **최상위로 뽑았다**
- **계열**: 철칙 4(PoB는 계산기이지 검증기가 아니다)의 또 다른 사례 —
  `insight.pob-is-a-calculator-not-a-validator`. 공급원 없는 config 건(#35·#36)과 동형
- **도구 감지 가능성**: **부분적**. `LifeCost == 0`인데 그 조건부 모드가 적용되는지는
  기계 판정 가능. PoB 수정은 불가(AD-1)이므로 **레코드에 조건 표시**가 현실적
- **착수 시**: 같은 꼴이 몇 건인지부터 센다(조건절이 붙은 노드 전량 대비)

### #21 KB 검증 실패가 **조회 전체를 차단**한다 (운영 사고)
- **상태**: **부분 해결** (2026-08-08) — 원인 표시 완료, **부분 로드 여부는 판정 대기**
- **사고**: `Life_Loss 2.json`·`Resistance_Penetration 2.json`(macOS 사본 패턴,
  **git 미추적**, 원본과 체크섬 동일)이 생겨 중복 id 2건 → **KB 조회가 전부 막혔다.**
  원인 찾는 데 몇 분 걸렸고 **무인 세션이었으면 그대로 멈췄을 것**
- **검증**: `store.py:254`가 중복 id를 `errors`에 쌓고 `load()`가 `KBValidationError`로
  **전량 실패**시킨다. 무관한 레코드 2건이 9,000여 건의 조회를 막는 구조가 맞다
- **도구 감지 가능성**: **가능(자동 보고)**. `git ls-files`로 미추적 여부를 판정할 수
  있으므로 메시지에 **"미추적 파일이 원인"**을 넣으면 즉시 해결된다
- **한 일** (2026-08-08):
  - `store._duplicate_id_error` — 중복 두 파일 중 **git 미추적 쪽을 지목**하고 정본이
    어느 쪽인지 말한다. 판정 불가(git 없음·저장소 아님)면 **아무 말도 하지 않는다**
  - ⚠ **옛 메시지는 사람을 반대 방향으로 보냈다**: 파일을 이름순으로 읽으므로
    사본(`Life_Loss 2.json`)이 먼저 등재되고 **원본이 "중복"으로 고발**됐다. 그대로
    따르면 진짜 레코드를 지운다. 이게 원인 찾는 데 몇 분이 걸린 진짜 이유다
  - 회귀: `tests/unit/test_kb_duplicate_hint.py` — 미추적 사본 지목 · **둘 다 추적본이면
    추측하지 않음** · git 아니어도 안 죽음
- **판정 대기 (사용자)**: **부분 로드**로 갈 것인가. 지금 조치는 사람이 몇 분 대신 몇 초에
  고치게 할 뿐, **무인 세션은 여전히 멈춘다.**
  - 제안: 중복 두 파일이 ① 한쪽이 미추적이고 ② **내용이 바이트 단위로 같으면**
    미추적 쪽을 **로드에서 제외**하고 경고만 남긴다. 사본이므로 잃는 정보가 없다
  - 반대 근거: 정본이 깨진 채 조회가 되는 것도 위험하다. 그리고 새 레코드를 만드는
    세션은 커밋 전이라 **정상적으로 미추적**이므로, 규칙이 넓으면 새 레코드가 조용히
    사라진다 — ②(내용 동일)가 그 방어선이다

### #22 **0.5 대표 크래프팅(퀄리티 축)이 통째로 PoB 미측정** — **부분 해결**
- **상태**: **부분 해결** (2026-08-09) — 접사 축은 보고 경로 완성, **임플리싯 축은 남음**
- **보고**: 사용자 지적 — *"에센스로 최대 퀄리티 +20%를 붙인 뒤 기폭제로 40%를 만들고
  그 옵션을 교체하면 퀄리티는 남는다. **이번 0.5 시즌 대표적인 크래프팅 방법**"*.
  KB에 부품은 전부 있는데(`item.breach-ring` 임플 +20% · `modifier.essencebreach` +20% ·
  `ringimplicitmaximumquality*` · 기폭제 12종) **전부 `pob_modeling.supported: false`**다
- **영향(보고)**: `optimize_rare`가 목걸이 접사 82건을 전량 실측하고도 퀄리티 축을 후보로
  못 올렸다 → **낸 희귀 수치가 전부 바닥값**이다. 40% 퀄리티면 `+3 주문 스킬 레벨`이
  **+4**가 되고 레벨당 실측 +2,539 IgniteDPS인데 계산에 아예 없었다
- **검증 (엔진 세션 2026-08-09, 부분)**: 그 레코드들에 `pob_modeling`이 실제로 붙어 있다 —
  **내가 PR #48에서 붙인 것**이다(`kind: item-line-unparsed`). 즉 "PoB가 못 읽는다"는
  이미 표기돼 있고, **그 표기를 `optimize_rare`가 안 읽는 것**이 이 건의 알맹이다
- **한 일** (2026-08-09):
  - `AffixOption.pob_unmeasurable` 신설 — 접사 풀이 KB 표기를 **읽는다**
  - `optimize_rare` 반환에 `unmeasurable`(전량) + **경고 노트**. MCP에도 노출
  - 실측: `Amber Amulet` 접사 풀 **82건 중 32건(39%)**이 PoB 미측정이다. 그리디가
    이것들을 절대 못 고르므로 **조립 결과는 바닥값**이고, 이제 그 사실이 나간다
- ⬜ **남은 것 — 퀄리티는 접사가 아니라 임플리싯이다.** 보고가 지목한
  `essencebreach`·`ringimplicitmaximumquality*`는 **임플리싯**이라 `optimize_rare`의
  접사 풀에 애초에 안 들어온다. "에센스로 +20% → 기폭제로 40% → 옵션 교체" 경로 자체가
  도구에 없다. 이건 별도 설계가 필요하다(임플리싯 후보 축)
- ⬜ `applicable_pages` 불일치 정리도 남음(위 부수 결함)
- **부수 결함 (검증됨)**: `applicable_pages`가 **일관되지 않다** —
  `modifier.essencebreach`는 `["Amulets","Rings"]`를 갖는데
  `ringimplicitmaximumqualityadditional1/2`·`…override1`은 **없다**(None).
  그래서 Ring 전용 여부를 `pob_key` 이름 추론에 의존해야 했고, 이 차이가 실제로
  판정을 갈랐다(목걸이 40% vs 균열 반지 60%)

### #23 `optimize_rare`가 **합법성 위반 조립**을 낸다
- **상태**: 미검증(보고 원문)
- **보고**: 투구(`Spiritbone Crown`) 실행 결과가 `legal: false` — group 중복
  (`IncreasedLife`) · 접두 4개(한도 3) · 접사 총 7개(한도 6). 도구가 **스스로 잡아내긴
  한다**(그건 옳다). 다만 사후 검사라 **반환 `text`를 그대로 못 쓰고** 매번 손으로
  재조립해야 했다
- **도구 감지**: **가능** — 조립 그리디에서 한도·`group` 배타를 지키며 위반 후보를
  건너뛰면 된다. **「거부」가 아니라 애초에 생성 안 함**

### #25 **이동속도가 어떤 가중치 축에도 없다** — **부분 해결**
- **상태**: **부분 해결** (2026-08-09) — 측정은 됨, **가치 정량화는 판정 사항**
- **보고**: 사용자 지적 — *"이속이 빠르면 맵핑 사이클을 더 돈다. 실제로 3원소 마법사의
  피는 **가장 낮은 가치로 거래된다**"*. 보고 세션은 Ruby·Sapphire·Topaz(3저항)로 짰는데,
  `IgniteDPS`·`TotalEHP` 어디에도 이동속도가 없어 **수은의 점수가 0**이었기 때문이다 —
  시장 가치와 정반대 결론
- **한 일**: #18의 방어 축 자동 측정에 **`MovementSpeedMod`를 포함**했다. 실측 확인:
  방랑벽 +0.200 · 경이의 덫 +0.150 등이 `defensive_only`로 나온다
- ⚠ **내가 먼저 틀렸다**: 처음에 축 이름을 `MovementSpeed`로 적었는데 PoB의 실제 키는
  **`MovementSpeedMod`**다. 후보 20종이 전부 `이동 0.000`으로 찍혔는데도 넘어갔고,
  이 이관 보고를 받고서야 알았다 — **고치려던 「조용한 0」에 그대로 빠진 것**이다.
  `tests/integration/test_axis_names_exist.py`가 축 이름 실재를 PoB 실측으로 잠근다
  (유닛으로는 못 잡는다 — 가짜 오라클은 준 키를 그대로 돌려준다)
- **남은 것(판정)**: 이동속도의 **가치**는 정량화가 어렵다(맵핑 처리량 = 딜×이속×생존).
  `weights`에 없으면 경고하는 것까지가 도구의 몫이고, 가중치를 얼마로 둘지는 설계 판단이다

### #4 아이템 부여 스킬과 젬 스킬이 구분되지 않음
- **상태**: 미착수 — 게임 지식 판정 일부 필요
- **실측 사례**: `skill.purity-of-fire`·`skill.malice`를 젬 오라로 오인 → 정신력 배분표에
  넣음. 실제로는 셉터 부여라 젬으로 못 켜고 **셉터당 하나만** 고를 수 있어 배타
- **단서는 있었으나 표식이 아님**: 젬 스킬은 `tier`·`reservation` 보유, 이 둘은 둘 다 없음
- **요청안**: `source: gem | item-granted | ascendancy-granted` + `granted_by: [item ids]`
  (Modifier엔 이미 있는 필드) + 부여 스킬의 **정신력 점유량 수집**
- **묶기**: `far_clusters` 작업과 함께

### #2 `acquisition: liquid-emotion`의 의미가 오독을 유발
- **상태**: 판정대기 (게임 지식) + 필드 설계
- **실측 오판**: "성유로만 얻는 무작위 노드 = 트리에서 못 찍음"으로 읽어 **틀린 설계
  판단**. 사용자 정정: "성유로 **부여도 가능**하다는 뜻이지 전용이 아니다.
  실제로 찍을 수 있는 노드다"
- **혼동 원인**: `liquid-emotion-only`(성유 전용, 트리 할당 불가 — `passive.dominion-26214`)가
  **따로 존재**하는데 이름이 비슷해 의미가 정반대인 줄 모른다
- **요청안**: `tree_allocatable: true/false` 명시 필드(파생 말고 정본), 또는
  `anointable`/`anoint-only`로 개명

### #5 `cost` 필드의 "미수록"과 "명시적 0"이 섞임
- **상태**: 미착수
- **실측**: `describe_type("Skill", field="cost")` 충전율 **63.8%**, 그중 명시적
  `{Mana, min:0, max:0}` **52건**. 나머지 36.2%는 미수록
- **실측 오판**: cost 필드 부재를 보고 "마나 소모 0"으로 단정했다가 철회
- **요청안**: 명시적 0을 전 스킬에 일관 적용하거나, 미수록을 `null`로 명시
- **선례**: 쿨다운은 이 방식으로 해결됨(`cooldown_s` 0 = 없음 / 부재 = 미수록)

### #29 〔신규〕 요구 속성 미달 경고가 **1회성**이라 20회 측정 동안 사라졌다 — **해결**
- **상태**: **해결** (엔진 세션 2026-08-09) — #27과 같은 반환 자리에 상시 부착
- **기록자 주**: 엔진 세션이 바쁜 시점이라 사용자가 **이 건 한정으로** 빌드 세션의
  직접 기재를 허용했다. 재현·대조는 착수 세션이 할 것

**무엇이 일어났나 (인과 사슬)**

1. `optimize_items` 반환 `notes[0]`에 경고가 나왔다:
   `⚠ 기반 스펙 자체가 요구 속성 미달: {'int': 112.0} — 트리·장비로 채워야 실제 착용이 성립한다`
2. 빌드 세션은 **그 턴에 읽고 사용자에게 보고까지 했다**("놓치고 있던 경고")
3. 그 뒤 `compute_pob`·`evaluate_delta`·`optimize_rare`·`evaluate_change_bundle`을
   **약 20회** 돌리며 트리 120포인트와 장비 10슬롯을 확정했다
4. **그 20회 중 경고가 다시 나온 적이 한 번도 없다.** 전부 `tree_legal: true`를 반환했다
5. 최종 조립에서 `Int`를 직접 stats에 요청해서야 **58**임을 발견했다 —
   주무기 `Runic Fork`(Lv65, **Int 114**) 요구 미달로 **착용 불가 세팅**이었다

**구조적 원인 (문서 탓이 아니다)**

| 요인 | 내용 |
|---|---|
| 경고가 **1회성** | 발생 도구(`optimize_items`)의 그 호출에서만 난다. 이후 측정에 안 따라붙는다 |
| 주력 측정 도구에 필드 부재 | `req_shortfall`이 `optimize_items`·`optimize_rare`엔 있는데 **`compute_pob`·`evaluate_delta`엔 없다.** 설계 반복은 이 둘로 한다 |
| `tree_legal`이 오해를 부른다 | 트리 연결성인데 이름이 "이 빌드가 합법"으로 읽힌다. 착용 불가인데 20회 내내 `true`였다 |
| 문서 규율은 이미 있었다 | `AGENTS.md` 철칙 4 「측정값 ≠ 실현 가능성」 · `insight.pob-is-a-calculator-not-a-validator`. 빌드 세션은 이 규율을 **알고 인용까지 했으면서** 어겼다 |

**교훈**: `insight.disciplines-need-enforcement-points`의 재발이다. 다만 이번 건은
①**거부** ②**자동 보고** ③문서 중 ②가 **이미 구현돼 있었는데도** 실패했다 —
**한 번만 보고했기 때문**이다. 자동 보고는 *발생 시점*이 아니라 **위반이 지속되는 동안
계속** 나와야 한다. 1회성 경고는 문서와 같은 강제력을 갖는다

**요청안**
1. `compute_pob`·`evaluate_delta`·`evaluate_change_bundle` 반환에 **`req_shortfall`
   상시 부착** — 빈 dict여도 키를 남겨 "검사했고 미달 없음"이 보이게
2. 미달 시 `tree_legal`과 **별개 플래그**(`wearable: false` 등). 지금은 트리 합법성이
   장비 착용 가능성을 가리고 있다
3. `tree_legal` → `tree_connected` 개명 검토 — 이름이 실제보다 넓은 보증으로 읽힌다
4. **회귀 테스트**: Int 요구 114 무기를 Int 58 스펙에 넣고 `compute_pob`을 부르면
   `req_shortfall: {"int": 56}`이 반환에 있는가

**근거 위치**: `optimize_items` 반환 `notes`(요구 속성 미달 문구) ·
`item.runemastered-runic-fork` 요구치 · `AGENTS.md` 철칙 4 ·
`insight.pob-is-a-calculator-not-a-validator` · `insight.disciplines-need-enforcement-points`

### #26 〔신규〕 `assemble_pob`가 **어센던시 빌드를 하나도 조립할 수 없다** — **해결**
- **상태**: **해결** (엔진 세션 2026-08-09) — 교착 재현 → 판정식 교체 → 회귀
- **엔진 세션 재현 (2026-08-09, Witch1)**: 보고대로 **어느 쪽으로도 통과 불가**였다.
  시작 노드 빼면 `check_ascendancy_entry`가 거부, 넣으면 `pruned=(32699,)` ·
  `is_tree_legal=False`. 빼고 돌린 PoB는 `allocAscendancy=6`으로 **정상 배정**했다 —
  즉 PoB는 시작 노드를 스스로 넣고 포인트로 세지 않는다는 보고가 맞다
- **한 일**:
  1. 판정식을 **「목록에 있는가」 → 「시작 노드에서 할당 노드만 밟아 닿는가」**로 교체.
     시작 노드는 `tree_nodes`에 **요구하지 않는다**(넣어도 통과 — 넣는 게 틀린 건 아니다)
  2. `pruned_nodes` 메시지가 **왜 잘렸는지** 구분한다 — 「PoB가 자동 배정하는 시작
     노드라 뺄 것」과 「진짜 비연결」은 원인이 다른데 같은 문구로 나왔다(요청안 3)
  3. 회귀 2종: 시작 노드 없는 정상 배치가 **통과**하는가 / 시작 노드에서 **끊긴**
     노드가 거부되는가. 전자가 이 교착의 회귀다
- ⚠ **기존 테스트 2건이 옛 판정식을 고정하고 있었다** — 그래서 이 결함이 회귀로
  잡히지 않았다. 「어센던시 진입」 차단을 기대하던 단정을 정정했다(오거부였다)
- **기록자 주**: #29와 같은 경위로 사용자가 직접 기재를 지시했다(2026-08-09)
- **영향**: **차단성**. 어센던시 노드를 하나라도 찍은 빌드는 `assemble_pob`를 통과하지
  못한다. 조립이 막히면 아이템 적법성 검사(#27)도 같이 못 돌아 **정본 경로가 통째로 끊긴다**

**두 검사가 서로를 막는다**

| 검사 | 요구 |
|---|---|
| `assumptions.py:370 check_ascendancy_entry` (2026-08-06 추가) | 어센던시 시작 노드를 **`tree_nodes`에 넣으라** |
| `assemble.py:80` ← `runner.pruned_nodes` | PoB가 시작 노드를 **auto-alloc**하므로 목록에 넣으면 **잘라낸다** → 비연결 판정 |

실측(2026-08-09, Witch1/인퍼널리스트):
- 32699 **빼고** 호출 → `ok:false` `어센던시 노드 4개를 찍었는데 시작 노드 32699가 없다`
- 32699 **넣고** 호출 → `ok:false` `트리 비연결 노드: (32699,)`
- `compute_pob`으로 확인 → `pruned_nodes: [32699]`, `allocAscendancy`는 **넣든 빼든 4**.
  즉 PoB는 시작 노드를 스스로 배정하고 **포인트로 세지 않는다**(`points.py:3` 주석과 일치)
- 기존 성공 아티팩트 `20260802-ember-fusillade-infernalist-branch-a-smoke/build.xml`의
  `nodes=`에 **32699가 없다** — 가드가 동작하던 빌드보다 나중에 추가됐다는 증거

**가드의 의도는 옳다, 판정식이 틀렸다.** 2026-08-06 사고(시작 노드 없이 어센던시 9개)의
진짜 결함은 "목록에 시작 노드가 없다"가 아니라 **"어센던시 노드가 시작 노드에서 안 닿는다"**다.
목록 포함 여부로 그걸 대신 재려다 PoB 모델과 어긋났다

**요청안**
1. 판정식 교체 — 어센던시 서브그래프에서 **시작 노드로부터의 도달 가능성**으로 검사한다.
   시작 노드는 `tree_nodes`에서 **요구하지 않는다**(PoB가 배정한다)
2. 회귀 테스트: 시작 노드를 뺀 정상 인퍼널리스트 트리가 `assemble_pob`를 **통과**할 것 /
   시작 노드에서 끊긴 어센던시 노드가 있으면 **거부**할 것. 지금 테스트는 전자를 못 잡는다
3. `pruned_nodes` 메시지에 **왜 잘렸는지**를 붙일 것 — "비연결"과 "PoB가 자동 배정하는
   노드라 목록에 못 넣음"은 원인이 다른데 같은 문구로 나온다

**실측 대체 구현(참고)**: 시작 32699 → `63484 인화성 강도 → 18158 화염의 인도자` ·
`24135 치명타 확률 → 34419 웃는 번제`. `TreeGraph.adj`로 BFS하면 4노드 전부 도달 —
이 경로로 조립하니 `적법 True`로 통과했다

**근거 위치**: `src/pok/engine/constraints/assumptions.py:370-403` ·
`src/pok/engine/assemble.py:78-80` · `src/pok/engine/tree/graph.py:119-131` ·
`src/pok/engine/constraints/points.py:3-5` · `passive.infernalist-32699`

### #27 〔신규〕 `compute_pob`가 **아이템 적법성을 검사하지 않는다** — **해결**
- **상태**: **해결** (엔진 세션 2026-08-09) — #29와 같은 자리라 함께 고쳤다
- **계열**: #29와 **같은 뿌리**다. `compute_pob`는 트리만 보고 **장비는 통과시킨다**

**무엇이 일어났나**

설계 반복을 전부 `compute_pob`으로 돌렸고, 전 회차 `tree_legal: true`였다. 같은 스펙을
`assemble()`(= `ItemLegalityChecker` 전 슬롯 통과)로 넘기자 **10슬롯 중 4개가 실격**이었다:

| 슬롯 | 판정 | 실제 |
|---|---|---|
| 장갑 | `베이스 미확인: 'Silk Gloves'` | **존재하지 않는 베이스**. ES 계열 정답은 `Jewelled Gloves`(`int_armour`, ES 23) |
| 신발·장갑·투구 | `ILLEGAL` 티어 범위 밖 | `LocalArmourAndEnergyShieldAndStunThreshold`의 `spawn_weights`는 **gloves 0·helmet 0·body_armour 0**, `str_int_armour`에만 1. 순수 ES 베이스엔 **애초에 안 붙는 접사**다 |
| 목걸이 | `UNKNOWN` ×2 | `12% more Elemental Damage with Spells`·`50% more Flammability Magnitude` — **둘 다 실재하지 않는 문구**(실제는 `increased Flammability Magnitude`) |
| 허리띠 | `UNKNOWN` | `Has 3 Charm Slots` → `item.mageblood` 실제 implicit은 `Has (1-3) Charm Slot` |

그 위에서 나온 `IgniteDPS 66,702` · `TotalEHP 7,420`이 **설계 판단의 근거로 20여 회 쓰였다**

**구조적 원인**

| 요인 | 내용 |
|---|---|
| 검사기가 **조립 경로에만** 있다 | `assemble.py:62-76`이 `ItemLegalityChecker`를 돌린다. `compute_pob`은 안 부른다 |
| 그런데 설계 반복은 **`compute_pob`으로 한다** | `evaluate_delta`·`evaluate_change_bundle`·`optimize_*` 전부 같은 경로. 즉 **검사가 걸린 도구를 설계 중엔 안 쓴다** |
| `tree_legal`이 또 가린다 | #29와 동일 — 이름이 "이 빌드가 합법"으로 읽히는데 장비는 안 본 값이다 |
| 조립이 막혀 있었다 | #26 때문에 `assemble_pob`가 거부만 해서, **정본 경로로 넘어갈 방법 자체가 없었다** |

**사용자 판정(2026-08-09)**: *"compute 스킬은 간단한 계산이나 초기 빌드 선정 과정의
대략적 예측에만 쓰는 게 좋겠다. 빌드 컨셉 확정 후 본격적으로 만드는 과정에는 적합하지 않다."*
→ 도구의 **용도 경계**를 문서가 아니라 반환값에 새길 것

**요청안**
1. `compute_pob`·`evaluate_delta`·`evaluate_change_bundle` 반환에 **아이템 적법성 요약
   상시 부착** — `items_legal: bool` + 실격 슬롯·사유. #29 요청안 1(`req_shortfall` 상시
   부착)과 **같은 수정 지점**이므로 묶어서 처리
2. 실격이 있으면 스탯 옆에 **경고를 매 회차 붙일 것**(#29 교훈: 1회성 경고 = 문서와 동급)
3. `tree_legal` → `tree_connected` 개명(#29 요청안 3과 동일 건)
4. 회귀 테스트: 없는 베이스(`Silk Gloves`)와 없는 문구를 담은 스펙으로 `compute_pob`을
   부르면 반환에 실격이 **드러나는가**

**근거 위치**: `src/pok/engine/assemble.py:62-76`(검사 위치) · `src/pok/mcp/tools/build.py:125`
(`compute_pob`은 `_compute`만 부른다) · `item.jewelled-gloves` · `item.mageblood` ·
`modifier.localarmourandenergyshieldandstunthreshold1`(`spawn_weights`)

### #28 〔신규〕 장갑에 넣은 화염 저항이 **조용히 0**으로 반영됐다
- **상태**: 미검증 (빌드 세션 관찰, 2026-08-09) — **가짜 장비(#27)가 원인일 수 있어 재현 필요**
- **관찰**: 같은 줄 `+40% to Fire Resistance`를 **장갑에 넣으면 `FireResist` 42 그대로**,
  **신발에 넣으면 75(캡)**. 둘 다 5줄짜리 희귀였고 다른 변수는 동일했다
- **주의**: 이후 그 장갑이 `베이스 미확인: 'Silk Gloves'`로 실격 판정을 받았다(#27).
  **베이스 미확인 아이템의 모드를 PoB가 통째로 버렸을 가능성**이 크다 — 그렇다면
  별건 결함이 아니라 **#27의 증상**이고, 진짜 문제는 *조용히* 버렸다는 것이다
- **착수 시 확인**: 적법한 ES 베이스(`Jewelled Gloves`)로 바꿔 재현되는지부터.
  재현 안 되면 #27로 흡수하되, **"베이스 불명이면 모드를 버리되 말은 하라"**를 요청안에 추가

### #30 〔신규〕 적법성 검사기가 **PoB 소켓 문법을 모드로 오독**한다 — **해결**
- **상태**: **해결** (엔진 세션 2026-08-09) — 재현 후 스펙 줄·표식을 판정에서 제외
- **증상**: 룬을 채운 정상 빌드가 `적법 False`로 찍힌다. 실격 사유가 전부 소켓 선언이다:
  ```
  UNKNOWN: Rune: Perfect Iron Rune — KB에 일치하는 모드 텍스트 없음
  UNKNOWN: Sockets: S S S S S
  UNKNOWN: 1% increased Energy Shield — `{rune}` 표기지만 룬 풀에 일치 없음
  UNKNOWN: Corrupted — KB에 일치하는 모드 텍스트 없음
  ```
- **사실**: `Sockets:`·`Rune:`은 PoB의 **스펙 줄**(`Item.lua:570-580`, `specName == "Rune"`)이지
  모드 줄이 아니다. `Corrupted`도 표식이다. PoB는 정상적으로 읽어 계산까지 했다 —
  이 빌드에서 룬 21칸이 **IgniteDPS +69.6%**(48,601 → 82,416)를 냈다
- **왜 나쁜가**: 정상을 비적법으로 찍으면 **경고가 신호를 잃는다**. #27이 잡아낸 진짜 실격
  4건과 이 오탐 6건이 같은 목록에 섞여 나왔다
- **요청안**: 검사기가 스펙 줄(`Sockets:`·`Rune:`·`Radius:`)과 표식(`Corrupted`)을
  모드 판정에서 제외할 것. 대신 `Rune:` 값은 PoB `data.itemMods.Runes` 실재 여부로 검사

### #31 〔신규〕 룬 줄의 **수치를 아무도 검증하지 않는다** — **해결**
- **상태**: **해결** (엔진 세션 2026-08-09) — 소켓 수·룬 효과로 상한을 계산해 대조
- **실측 사고**: 완드에 적힌 3줄이 전부 실제 룬 값의 배수였는데 `check_item_legality`가
  `CONDITIONAL`(= 룬으로는 가능)로 통과시켰다:

  | 적힌 값 | 실제 룬 | 배율 |
  |---|---|---|
  | `+9 to Level of all Spell Skills` | Hedgewitch Assandra's Rune of Wisdom = **+1** | ×9 |
  | `150% increased Spell Damage` | Greater Iron Rune (wand) = **30%** | ×5 |
  | `120% increased Spell Damage` | 같은 룬 = **30%** | ×4 |

- **검사기가 보는 것**: 문구가 룬 풀에 있는가 **뿐**. 값 범위를 안 본다.
  일반 접사에는 티어 범위 검사가 있다(#27에서 `96% increased Armour and Energy Shield`를
  "티어 범위 밖"으로 잡았다) — **룬에만 없다**
- ⚠ **다만 이 건의 원본 수치는 결과적으로 정당했다**: 무기가 유니크
  **`Runeseeker's Call`**(base `Runic Fork`, 룬 소켓 **5칸**, `200% increased effect of
  Socketed Runes` = ×3)이었다. (1+2)×3 = 9가 맞는다. 즉 **값 자체는 성립하는데
  검사기는 그걸 확인해서 통과시킨 게 아니라 안 보고 통과시켰다** — 우연히 맞은 것이다
- **요청안**: 룬 줄은 `data.itemMods.Runes[name]`의 부위별 값 × 아이템의
  `SocketedRuneEffect`로 **재계산해 대조**할 것. 소켓 수 초과도 함께(21칸 배치 중
  베이스 한도를 넘겼는지 아무도 안 봤다)

### #32 〔신규〕 KB에 **베이스 implicit이 통째로 없다** — 유니크 오판의 원인
- **상태**: 원인규명 (빌드 세션, 2026-08-09) · **검증: 전수 확인**
- **실측**: 완드 **16종 전부** `implicits: None`, 목걸이 **23종 전부** `implicits: None`.
  PoB 원본에는 있다(`Data/Bases/wand.lua` — `Runemastered Runic Fork`는 변종 3개가
  각각 다른 implicit을 갖는다)
- **결과**: 베이스 선택을 KB로 가를 수 없다. 그리고 이 갭 때문에 빌드 세션이
  `Runeseeker's Call`의 존재를 못 보고 **정상 아이템을 "지어낸 수치"로 오판**해
  희귀 쇠스랑으로 갈아치웠다(딜 −41%). 사용자 지적으로 되돌렸다
- **요청안**: ingest에서 `Data/Bases/*.lua`의 `implicit`·`implicitModTypes`를 수록.
  변종이 여럿인 베이스(동명 3개)는 **전부** 실어야 한다 — 하나만 실으면 조용히 틀린다

### #33 〔신규〕 최적화기가 **룬 소켓을 아예 고려하지 않는다** — **해결**
- **상태**: **해결** (엔진 세션 2026-08-09) — `optimize_runes` 신설
- **엔진 세션 검증**: 표기 함정을 **직접 재현했다**. 완드 + `200% increased effect of
  Socketed Runes`에서 손기입 `{rune}` Δ+26.5 vs `Sockets:`+`Rune:` 선언 Δ+79.4 —
  정확히 **3.00배**. 그리고 `Rune:`만 있고 시드 줄이 없으면 **Δ 0**이다(원시 텍스트에서
  PoB는 룬 문구를 스스로 채우지 않는다). 즉 **셋 다** 필요하다
- **한 일**: `engine/runes.py` — 후보 열거(`Bonded:` 제외) · `render_runed`(선언 형식
  강제) · `check_rune_rules`(유산 1 · 고유명 1 · 일반 중복) · `optimize_runes`(실측
  그리디). MCP `optimize_runes`로 노출. 실측: 완드 3칸에 룬 56종을 재서 채움
- **남은 것**: `optimize_items`가 슬롯을 돌 때 **자동으로** 룬까지 채우게 묶는 것.
  지금은 슬롯 단위 도구라 호출자가 돌려야 한다(유산 1개 규칙도 호출자가 관리)
- **실측 규모**: 룬 21칸을 채우자 **IgniteDPS 48,601 → 82,416 (+69.6%)** ·
  EHP 16,271 → 20,724 (+27.4%). 그런데 `optimize_rare`·`optimize_items` 출력에는
  룬이 **한 줄도 없다** — 접사만 조립한다
- **왜 자동이어야 하나**: 백로그에 이미 같은 사고가 있다(*"16칸 0% 사용에도 5종 통과,
  나중에 채우자 DPS +37~47%"*). 두 번째다. `check_constraints(exhaustion.sockets)`는
  **미사용을 보고만** 하고 채워주지 않는다 — 그래서 두 번 다 빈 채로 넘어갔다
- **비용이 낮다**: 후보 수가 작다. 실측에서 부위별 「효과 있는 룬」은 장갑 4 · 투구 6 ·
  신발 4 · 갑옷 11 · 집중구 1건뿐이었다(나머지는 전부 델타 0)
- **제약 규칙(사용자 확인 2026-08-09)** — 최적화기가 지켜야 한다:
  - **유산(`Legacy of …`)은 전 장비 통틀어 1개**
  - **고유명 룬은 같은 이름 1개**, 다른 이름은 병용 가능
  - 일반(`Lesser/Greater/Perfect … Rune`)은 **중복 가능**
- ⚠ **모델링 함정 (반드시 요청안에 포함)**: `{rune}` 접두를 **손으로 적으면 증폭이
  조용히 빠진다.** PoB는 `modLine.augmentType == "Rune"`일 때만
  `socketedRuneEffectModifier`를 곱하는데(`Item.lua:2198-2209`), 그 값은
  `UpdateRunes()`가 붙인다. 그래서 반드시
  `Sockets: S S …` + `Rune: <이름>` + `{rune}` 시드 1줄 형식이어야 한다.
  실측: 손기입 `{rune}35% increased Spell Damage`는 200% 줄이 있으나 없으나 Δ391로
  동일했고, `Rune:` 형식으로 바꾸자 **Δ1,190 (×3.04)**가 나왔다.
  **틀린 형식은 오류가 아니라 「조용한 과소 계상」으로 나온다**
- **근거 위치**: `external/pob/<pin>/src/Classes/Item.lua:570-580`(스펙 파싱) ·
  `:1046-1058`(UpdateRunes 조건) · `:2192-2209`(증폭 적용) ·
  `src/Data/Uniques/wand.lua`(Runeseeker's Call)

---


---

### 아래는 **해결됨** — 조사 기록으로 보존한다

§4의 표가 "무엇을 어떻게 고쳤나"의 요약이고, 여기 본문은 **어떻게 알아냈나**의 기록이다.
지우지 않는 이유: 같은 계열이 재발했을 때 "그때 무엇을 확인했고 무엇을 가정했는가"가
조사를 처음부터 다시 하지 않게 해 준다(기재 규약의 `검증` 칸이 존재하는 이유).

### #12 KB 한글 텍스트 오염 — **최우선**
- **상태**: **해결** — PR #42 머지. ⚠ 아래 「할 일」은 착수 당시 계획이고
  실제 조치 범위는 PR #42를 볼 것 (이 본문은 조사 기록으로 보존)
- **실측**: `texts`/`texts_ko` 줄 수 불일치 **1,536건**. `radius-grant` 플래그 519건
  중 **비반경 오부착 399건**
- **인과** (399/399 확인 — 우연 아님):
  ```
  ① texts_ko에 옆 모드 줄 혼입 (수집 파서)
  ② radius-grant 스캐너가 **한글 줄**에 매칭  (pob_gaps.py:141)
  ③ pob_modeling이 비반경 399건에 오부착
  ④ 세션이 "주얼 소켓은 PoB에서 저평가"로 읽고 **측정 방법론 변경**
  ```
- **검증**: ①~④ 전부 재현. ④는 실사용 세션이 실제로 겪음(최적화 회차 2배 증가 직전)
- **근거 위치**: `src/pok/kb/ingest/mods.py`·`mod_catalog.py`(수집),
  `src/pok/kb/pob_gaps.py:141`(정규식), 표본 `modifier.specialcorruptionareaofeffect1`
- **주의**: `radius-grant` 519건은 **이전 엔진 세션이 붙이고 "실규모"라 기록**했다.
  검증되지 않은 값이다 — 신뢰하지 말 것
- **할 일**: 한글 파서 수정 → 스캐너를 영문 기준으로 → 오부착 정리(`allow_drop` 근거)
  → **줄 수 일치 검사를 게이트에**(철칙 5 — 문서 말고 도구로)

### #13 해금 노드 배제가 **조회 계층에 없다** — 설계 단계에서 오판을 만든다
- **상태**: **해결** — PR #43 머지 (`[엔진] 해금 노드 배제 조회 계층` 세션, 2026-08-07).
  `find_clusters` 기본 배제(할 일 4)만 제안 A 구현 대기로 남는다
- **막힌 지점(보고 원문)**: 밀집도 스캔으로 「힘 소진」(`passive.exhaust-all-power-11428`,
  집정관 버프로 치명타 확률 30%·피해 50%)을 **인퍼널리스트 빌드의 치명타 병목 해법으로
  사용자에게 제시**하고, 「지배」 성유와의 상쇄까지 설계 근거로 세웠다. **오라클 전용
  해금 노드였다**(`unlock_constraint: {ascendancy: Oracle, nodes: [5571]}`).
  `stats`의 「보이지 않는 길 필요」를 읽고도 흘렸다.
- **실측(보고)**: 관련성 필터를 통과한 오라클 전용 노터블 **44개**. 배제 전 최적
  클러스터 (-2588,-7108)·13개 → 배제 후 **(-1682,-9147)·20개**. **필터 하나로 최적해의
  중심 좌표가 이동한다** — 개수만 바뀌는 게 아니다.
- **검증 (엔진 세션, 2026-08-07)** — 보고와 **일부 다름**:
  - ✅ `search_kb`에 배제 없음 — `search(query="힘 소진", type_="Passive")`가 그대로
    반환한다. **여기가 실제로 밟은 틈이다.** `ascendancy` 인자는 "그 전직 **소유**
    노드"만 거르지 "다른 전직 **해금**이 걸린 일반 트리 노드"는 안 거른다
  - ❌ "차단이 `assemble_pob`에만 있다"는 **부정확**. `TreeGraph.candidates()`는
    이미 배제한다(PR #35). 실측: `candidates`에 잠긴 노드 포함 = **False**
  - ⚠ 다만 `optimize_tree`가 `ascendancy_name`을 **안 넘긴다** → 기본 None →
    **잠긴 노드를 전량 배제**한다. 안전하지만 **과잉**이다: 오라클 빌드가 자기
    노드를 후보로 못 받는다
  - ❌ "177건 중 1건 필드 없음"은 **재현 안 됨**. 실측 **176건 전부 보유**, 누락 0건
- ⚠ **위 검증의 마지막 줄도 틀렸다** (착수 검증 2026-08-07, 아래 「신규 결함」).
  176이 아니라 **179건**이고, 그중 **3건은 `ascendancy`가 없다**
- **도구 감지 가능성**: **가능(거부 칸)**. `data.unlock_constraint`가 `{ascendancy,
  nodes}` 구조로 정본에 있어 문자열 파싱 불필요 — 스펙의 `ascendancy`와 대조만 하면 된다
- **한 일** (PR #43, 2026-08-07):
  1. ✅ `search_kb`에 `for_ascendancy` 인자 — **빼지 않고 `excluded_by_unlock` 사유를
     싣는다**. 히트에 `locked_to`·`requires_nodes`도 붙는다. 제약이 없으면 세 칸을
     **생략**해 압축 히트(D14)의 토큰 비용을 0으로 둔다.
     인덱스에 `records.unlock` 칼럼 신설(SCHEMA_VERSION 6 — self-healing이 재빌드)
  2. ✅ `optimize_tree` → `candidates(ascendancy_name=spec.ascendancy)`. 과잉 배제 해소
     실측: 오라클 빌드가 못 보던 자기 노터블 **42개**가 후보로 돌아온다
  3. ✅ `TreeGraph.resolve_ascendancy()` — 코드(`Witch1`)·영문·한글 아무 표기나 실명으로.
     **스펙은 코드를 들고 `unlock_constraint`는 실명이라 잇지 못하면 대조가 성립하지
     않는다.** 모르는 표기는 원문 폴백(None으로 떨구면 "제약 없음"으로 읽혀 샌다)
  4. ⬜ `find_clusters` 기본 배제 — 제안 A 구현 시 (미구현이라 이번 범위 밖)
- **교훈**: **차단이 파이프라인 끝(`assemble_pob`)에만 있으면 세션은 그 전에 이미 잘못된
  설계 결론을 낸다.** 조립 시점에 거부당하면 설계를 되감아야 한다. 후보를 내는 **모든
  지점**에서 같은 판정이 나와야 한다.

### #13-b 〔신규〕 선행 노드 요구형 해금이 **어느 검사에도 없었다**
- **상태**: **해결** — PR #43 머지 (2026-08-07, #13 착수 검증 중 발견)
- **무엇**: `unlock_constraint`에 `ascendancy` 없이 `nodes`만 있는 꼴이 있다. "다른 전직
  전용"이 아니라 **"이 노터블들을 먼저 찍어야 열린다"**. 전직 대조만 하던 검사는
  `locked_to`가 None이라 **그냥 통과시켰다**
- **실측**: Passive `unlock_constraint` 보유 **179건** = 전직 전용 176 + 선행 요구 **3건**
  (`passive.path-of-the-renegade-51850` 탈주자의 길 / `passive.the-hollowkeeper-42078`
  동혈수호자 / `passive.sacred-unity-28254` 신성한 합일). 앞 2건은 **본 트리** 노드라
  `candidates()`에 실제로 샜다(실측: 반경 40에서 2건)
- **왜 위험한가**: 「탈주자의 길」은 노터블 3개(고요한 바람단의 민첩함·부패한 염수단의
  흉포함·적검단의 절제)를 먼저 찍어야 열리는데, 안 찍고 할당해도 **PoB는 카오스 저항
  +8%를 그대로 더해 준다**. #13과 정확히 같은 결함 계열이고 게이트에 구멍이었다
- **조치**: `TreeNode.requires_nodes` 신설 → ① `candidates()`가 선행 전량이 `near`에
  있을 때만 후보로 냄 ② `check_locked_nodes`가 미할당 선행을 **거부**(`missing_nodes`)
  ③ `search_kb`가 `requires_nodes`로 표시

### #14 「보이지 않는 길」 — **레코드는 있었다. 순위가 문제였다**
- **상태**: **해결** — PR #43 머지 (검증·수정 2026-08-07)
- **보고**: `search_kb("보이지 않는 길")`이 엉뚱한 결과(동료 공격 속도·방어도)만 내고
  해금 조건 레코드가 안 잡힌다. 노드 5571이 무엇인지도 확인 못 했다
- **검증 — 보고의 결론은 틀렸다**: 노드 5571은 **있다**.
  `passive.the-unseen-path-5571` 「보이지 않는 길」(`ascendancy: Druid1` = Oracle).
  `unlock_constraint.nodes`가 가리키는 선행 노드도 **9개 전부 존재**한다
- **진짜 원인 — 관련성 순위가 없었다**: 질의가 177건에 매칭됐다. 오라클 전용 176건이
  `stats`에 **"보이지 않는 길 필요"**를 달고 있어서다. 그런데 정렬이 `ORDER BY r.id`라
  정작 그 노드가 **164번째**로 밀렸다. 세션은 상위 10건만 보고 "KB에 없다"로 결론냈다
- **조치**: 질의가 있을 때 **이름 정확 일치 → 이름 부분 일치 → 나머지** 순으로 정렬.
  실측: 두 질의(한글·영문) 모두 `passive.the-unseen-path-5571`이 **1위**로 올라온다
- **교훈**: 0건 진단(`empty`/`why`)은 **0건일 때만** 돈다. 이 건은 177건이 왔고 전부
  무관해 보였다 — **"많이 왔는데 다 엉뚱하다"는 침묵의 실패**였고 진단이 걸리지 않았다

### #15 〔신규·해결〕 키워드 수집이 **파일명 알파벳 순서에 따라** 정의를 버렸다
- **상태**: **해결** (2026-08-08, #3 「poe2db에 있나」 확인 중 발견)
- **무엇**: `collect_links`가 슬러그당 **처음 만난 링크만** 취했다(`if slug not in out`).
  그런데 같은 키워드라도 페이지마다 `data-hover`가 다르다 — 스킬 페이지는 CDN 팝업
  조각(`https://…`)을 주는데 **아이템 분류 페이지**(`Amulets`·`Charms`·`Bows`)는
  **검색 쿼리**(`?s=Data\KeywordPopups/…`)를 준다. 후자는 정의가 아니라서 fetch가
  건너뛴다. 즉 **어느 키워드가 수집되는지를 파일명 알파벳 순서가 정했다**
- **실측**: 슬러그 493개 중 — 정상 수집 257 · **살릴 수 있는데 버려짐 90** ·
  진짜 팝업 없음 146. 버려진 90개에 **`Archon_Buff`**가 있었고 거기 #3이 찾던
  지속 10초·회복 20초가 있었다. **#3을 "출처 갭"으로 오분류시킬 뻔한 원인**
- **2차 결함**: 일부 스냅샷 페이지는 숫자가 `#`로 치환돼 온다(`cdn.poe#db.tw`).
  http로 시작해 정상처럼 보이지만 호스트가 안 풀려 **DNS 오류로 죽는다** —
  3건(`Freeze_Threshold`·`Maximum_Resistances`·`Recoup`)이 재시도 3회를 낭비했다.
  훼손본을 고르지 않게 하니 같은 슬러그의 성한 URL로 붙어 **전부 수집됐다**
- **조치**: `collect_links`가 **받아올 수 있는 후보를 우선**한다(`_usable`: http이고
  `#` 없음). 재수집 결과 키워드 정의 255 → **345**, 신규 Mechanic 레코드 **89건**,
  기존 레코드에 정의 문장 병합 **256건**. 실패 0
- **회귀**: `tests/unit/test_keywords.py` — 알파벳 앞 페이지가 열화된 링크를 줘도
  뒤 페이지의 정상 URL을 취하는지 (tmp 픽스처라 스냅샷 없이도 돈다)
- **교훈**: **선입선출은 조용한 선택이다.** 같은 대상에 후보가 여럿일 때 "먼저 온 것"은
  기준이 아니라 우연이고, 그 우연이 무엇을 수집할지 정한다

### #18 최적화기가 **순수 방어·유틸 유니크를 구조적으로 배제**한다
- **상태**: **해결** (2026-08-09) — 재현·수정·회귀 완료
- **보고 실측**: `item.mageblood` vs 좋은 희귀 벨트 — **TotalEHP 1,929.5 → 2,930.4
  (+51.9%)** · 이동속도 +42%p · 저항 냉기/번개 각 +62/+22 · **IgniteDPS 델타 정확히 0**
- **왜 구조적인가**: 딜 위주 `weights`(`{"IgniteDPS":1,…}`)를 주면 이 아이템 점수가 **0**이고
  그리디는 양수만 채택한다 → **후보에 올라도 절대 안 뽑힌다.** 사용자 지적: 여러 세션이
  고점 세팅에서 마법사의 피를 **항상** 무시했다
- **왜 "딜 가중 준 사람 책임"이 아닌가**: 딜 가중이 **기본 사용 패턴**이고, 그 패턴에서
  특정 부류가 통째로 안 보이는 것이 결함이다
- **도구 감지 가능성**: **가능(자동 보고)**. 후보 델타가 **딜 축 전부 0 · 방어 축 양수**면
  그 자체가 신호다 → `defensive_only_candidates` 칸으로 반환에 싣기(`axes`의 `empty`
  보고와 같은 방식). 또는 `weights`에 방어 축이 없을 때 경고
- **검증 (엔진 세션 2026-08-09)** — 갈랐던 질문에 답이 나왔다:
  - ✅ **후보에는 오른다. 채택에서 빠진다.** `enumerate_slot_uniques`가 전수 열거하고
    `evaluate_slot`이 실측까지 하는데, `items.py`의 `score > 0` 관문이 걸러낸다
    (`blocked`도 아니다 — 그냥 점수가 0이다)
  - ✅ 실측(허리띠 20종·`weights={"CombinedDPS":1}`): **채택 가능 후보 0건**인데
    그중 **12종이 EHP를 올렸다**(아홉 꼬리 고양이 +123 · 메긴노드 +90 · 헤드헌터 +84…)
  - ⚠ **보고의 마법사의 피 수치는 재현되지 않았다** — 빈 스펙에서는 EHP 델타가 **0**이다.
    효과가 **장착한 플라스크에 걸려 있어서**이고, 보고 세션은 플라스크 3종을 낀 빌드에서
    쟀다. 결함의 존재는 같지만 **그 아이템은 문맥 의존**이라는 게 추가로 드러났다
  - ✅ 근본 원인 하나 더: **방어 축을 애초에 재지도 않았다.** `measure = stats or
    tuple(weights)`라 딜 가중이면 딜만 측정된다 — "딜 0·방어 양수"를 **판정할 근거
    자체가 없었다**
- **한 일**:
  1. 방어 축(`TotalEHP`·`Life`·`EnergyShield`·`Armour`·`Evasion`·`MovementSpeed`)을
     **가중치와 무관하게 항상 측정**. PoB 1회가 619개 스탯을 주므로 **비용 0**
  2. 가중 축 0 · 방어 양수인 후보를 `defensive_only`로 **반환에 실어 보낸다**
     (채택은 여전히 가중치가 정한다 — AD-3). MCP `optimize_items`에도 노출
  3. `weights`에 방어 축이 하나도 없으면 **경고 노트**를 붙인다
  4. 8건 초과 시 절단하되 **절단 사실을 노트로** 남긴다(조용한 절단 금지)
- **회귀**: `tests/unit/test_items.py` — 방어 전용 후보가 **채택되지 않고 드러나는가** ·
  가중치에 없어도 방어 축이 측정되는가
- **교훈**: **점수 0은 "값어치 없음"이 아니라 "그 축을 안 봤음"이다.** #3(PoB가 못 읽어
  0)과 표면 증상이 같고 원인만 다르다 — 세션은 둘 다 "실측했더니 0"으로 읽는다
- ⚠ **그리고 나도 같은 함정에 빠졌다** (2026-08-09): 이 수정에서 축 이름을
  `MovementSpeed`로 적었는데 실제 키는 `MovementSpeedMod`다. 후보 20종이 전부
  `이동 0.000`으로 찍혔는데 **그걸 보고도 넘어갔다.** `[빌드]` 세션의 이관(#25)이
  잡아냈다 — **역할 분리가 실제로 교차 검증을 만든 사례**다(§운용).
  `tests/integration/test_axis_names_exist.py`로 잠갔다

### #24 `optimize_items`가 **Helmet 슬롯에서 죽는다** — **해결**
- **상태**: **해결** (2026-08-09). **보고의 원인 지목은 틀렸다**
- **증상(보고)**: `optimize_items(slots=[…"Helmet"…])` →
  `Error: items[1]: 'Thrillsteel'는 KB 유니크인데 옵션 줄이 하나도 없다`.
  `items[1]`은 호출자가 아니라 **도구가 생성한 후보**다. Amulet은 정상, **Helmet만** 막힌다
- ❌ **보고: "후보 텍스트 생성기가 explicits를 빠뜨린다"** → **아니다.**
  `render_unique("item.thrillsteel")`는 `Onslaught`를 정상적으로 출력한다(실측)
- ✅ **진짜 원인**: 검사기의 길이 문턱이다. `buildxml.py:349`의
  `any(len(n) >= 12 and n in body_norm …)` — `Onslaught`는 정규화 후 **9자**라 문턱에
  걸려 **비교 대상에서 통째로 빠진다.** 그 결과 "explicits가 하나도 없다"로 판정된다.
  문턱은 짧은 문구의 **오탐(false accept)**을 막으려던 것인데 **오거부**를 낳았다
- **실측 규모**: KB 유니크 전체에서 **1건**(`item.thrillsteel`)이 유발했다. 한 건이
  슬롯 하나를 통째로 막는다 — **후보 하나의 예외가 최적화 루프 전체를 죽이기** 때문이다
- **한 일**: 짧은 explicit을 부분 문자열이 아니라 **줄 단위 일치**로 본다
  (`buildxml.py`). 양쪽을 다 막는다 — 우연한 부분 일치도, 짧다는 이유의 탈락도 없다.
  게이트가 살아 있는지도 함께 잠갔다(옵션을 정말 안 적으면 여전히 거부)
- ⚠ **남은 취약성(별건)**: 후보 하나가 예외를 던지면 `optimize_items`가 통째로 죽는다.
  조용히 건너뛰면 「조용한 누락」이라 그대로 두는 게 맞을 수도 있으나, **어느 후보에서
  죽었는지**는 메시지에 있어야 한다. 미착수
- **계열**: 조용한 실패가 아니라 **시끄러운 오거부**다. #1(유니크 옵션 조용한 무시, PR #38)
  수정이 자기 후보를 거부한 것 — **게이트가 자기 도구를 막은 첫 사례**

## 2. 기능 제안 (실사용 세션 발의, 2026-08-07~09)

### 제안 A — `optimize_tree`에 밀집도·비연결 인식 **[권장 1순위]**
- **실측 근거**: 예산 87로 단독 실행 → **43포인트에서 조기 종료 · 9포인트 낭비 ·
  주얼 0개 채택 · 치명타 12.16%(목표 80%)**. 손으로 밀집도를 분석하니 병목을 정면으로
  푸는 노터블(인화성 강도 80% 등)이 나왔고 **전부 후보 반경 밖**
- **구조적 한계 2가지**: ① 먼 클러스터를 못 봄 ② 연결 비용을 **항상 지불한다고 가정**
  (제어된 변형·허무의 산물은 비연결 할당이라 **0**일 수 있다)
- **설계 결정 (엔진 세션 판단, 발의 세션 통보 완료)**: 발의는 "문서 관문으로 강제"였으나
  **자동 부착으로 바꾼다**(철칙 5). `optimize_tree`가 내부에서 스캔해 후보 반경 밖
  클러스터를 `far_clusters`로 **반환에 싣는다**. `unconnected_regions` 인자 추가.
  `find_clusters`는 독립 도구로도 노출하되 **강제는 자동 부착으로**
- **알고리즘** (발의 세션이 Bash로 즉석 구현해 실효 확인): 전 노터블의 `position`을
  후보 중심으로 놓고, 각 반경 내 포함 노터블을 세어 `(개수, 관련성 점수합)` 최대인
  중심을 고른다. O(n²)이나 노터블 수천 규모라 즉시 끝난다.
- ⚠ **`exclude` 필터가 결정적이다.** 없이 돌렸더니 "반경 1000에 노터블 9개"가 나왔고
  그중 **3개가 도리깨 노드**였다. 관련성 필터 없는 결과는 쓰레기다. 인자 형태:
  `include=[(키워드, 가중치)]` · `exclude=[키워드]` · `radii=[1500,2200,3000,3800]` ·
  `min_score` · `exclude_ascendancy`
- **노터블을 효과 텍스트째로 반환할 것.** 「힘 소진」(집정관 버프로 치명타 확률 30%)과
  「지배」 성유(그 페널티를 무효화)의 상쇄는 **두 축을 동시에 봐야 보이는 조합**이다 —
  점수만 내면 영원히 못 찾는다.
- **비연결 할당 아이템 2종** (서로 다른 아이템이라 **동시 착용 가능**):
  - `item.controlled-metamorphosis` 제어된 변형 — 반경 **가변**(Very Small~Massive),
    대가 `모든 원소 저항 −(5-20)%`, `limited_to: 1`
  - `item.from-nothing` 허무의 산물 — 반경 **Small(1000) 고정**, 대가 없음, 타락,
    `limited_to: 1`
  - 실측 사례: 자기 번제(-11339,-2544)와 부싯돌 클러스터(-2588,-7108)가 **8,700 이상**
    떨어져 걸어서 이으면 통행 노드만 수십 포인트인데, 제어된 변형(Massive)이면 **0**
  - 인자 형태: `unconnected_regions=[{"center": [-2588,-7108], "radius": 3000}]`
    → 그 원 안의 노드는 **경로 비용 없이** 할당 가능한 것으로 계산

#### 추가 요청 (9차 이관 2026-08-08)

**`optimize_tree`에 `exclude_nodes` 인자** — 배타 감지는 어렵지만 이 인자는 순수 기능
추가라 즉시 가능하다. 실측 사고: 원소 집정관 축을 위해 「검은화염 계약」(화염 주문 100%를
카오스로 전환 — 집정관의 `Cannot deal Non-Elemental Damage with Spells`와 정면 충돌)을
손으로 뺐는데, `optimize_tree`를 다시 돌리자 남은 8포인트로 **그것과 「피의 제물」을
그대로 재채택**했다. PoB가 집정관을 모델링하지 않으니(#3) 충돌을 볼 방법이 없다.

#### ⚠ 반경 가정 정정 — **실측값** (엔진 세션 검증 2026-08-08)

발의 세션이 "Massive 3,800"으로 가정해 「제어된 변형으로 42포인트 절약」을 **두 번
보고했다가 철회**했다. 실제 값은 이렇다(`Modules/Data.lua:657-672`, 실효 = 표기 × 1.2 —
`gameConstants.PassiveTreeJewelDistanceMultiplier`):

| 라벨 | 표기 outer | **실효** |
|---|---|---|
| Small / Medium / Large | 1,000 / 1,150 / 1,300 | 1,200 / 1,380 / 1,560 |
| Very Large | 1,500 | **1,800** |
| Variable(최대) | inner 1,800 · outer 2,100 | **2,160 ~ 2,520** |

- **최대가 2,520**이다. 노리던 클러스터가 3,000~3,800 규모라 주얼 하나가 덮는 관련
  노터블은 **2개 남짓**이었다 → `find_clusters`는 반경을 **실제 주얼 반경 목록에서** 받아야 한다
- **Variable은 원이 아니라 도넛**이다(`inner` 존재 · PoB가 `innerSquared`도 계산한다).
  `unconnected_regions`를 원으로만 모델링하면 틀린다
- ✅ **보고자의 미확인 사항 해소**: `jewelRadii`에 `["0_1"]` 항목뿐이라 0.5에서 nil일까
  걱정했는데, `setJewelRadiiGlobally`(`Data.lua:627-643`)가 **트리 버전 이하 중 최신**을
  고르는 폴백이라 0.5는 `0_1` 표를 쓴다. **위 값이 0.5에 유효하다**

### 제안 B — 반경 주얼 소켓 평가
- **문제**: `Time-Lost` 접미는 **반경 내 노터블 수 × 부여값**이 가치인데, PoB
  `ModParser`에 패턴이 없어 파싱되지 않는다 → 서로 다른 소켓의 델타가 **동일**하게 나와
  "소켓 위치는 값어치 없음"으로 오독
- **방법**: KB `pob_modeling.workaround`가 이미 적어둠 — 반경 내 노터블 수를 세어 효과를
  텍스트로 합성 후 실측. 또는 `JewelSpec.allocates`로 편입
- **대상 모드**(`Time-Lost Diamond`, `sub_type: Radius`, `int_radius_jewel`):
  `jewelradiusspellcriticalchance`(반경 내 주요 패시브당 주문 치명타 (3-7)%) ·
  `jewelradiuscriticaldamage`((5-10)%) · `jewelradiusspellcriticaldamage`
- **증상 재확인용**: 실사용 세션에서 `optimize_tree`가 `jewels: []`로 **하나도 채택하지
  않았다** — 소켓별 델타가 동일하게 나온 것이 원인이다
- **선행**: **#12 해결 후**. 지금은 `pob_modeling`이 오염돼 판정 근거가 못 됨

### 제안 C — 좌표 대량 조회 경로 (도구 갭)
- **문제**: `AGENTS.md`가 파일 탐색을 금지하는데 **좌표를 관련성으로 거를 경로가 없어**
  세션이 `knowledge/**/*.ndjson`을 직접 읽는 것 외에 방법이 없었다
- **대안이 왜 없었나**: `get_entry` 380회는 비현실적 · `find_by_value("position.x")`는
  값 순 정렬만이라 **관련성으로 못 거른다** · `search_kb`는 압축 히트라 좌표를 반환하지
  않는다
- **원칙**: 도구 갭은 규율 위반을 강제한다 — **금지하려면 대안 경로를 먼저 만든다**
  (`insight.disciplines-need-enforcement-points` 따름정리). `find_clusters`가 생기면
  자연히 해소되지만, 그 전까지는 **규율이 지켜질 수 없는 상태**임을 기록해 둔다

### 제안 D — **PoB 파싱 갭 감사기** — ✅ **구현됨** (트리 한정, 2026-08-08)

- **문제의 일반형**: #3(집정관)은 특수 사례가 아니다. **KB에는 있는 효과 문구를 PoB가
  계산하지 않으면 델타가 조용히 0이 되고**, 세션은 그것을 "값어치 없음"이라는
  **실측으로 오독한다**. 집정관에서만 노드·룬·성유·접미 여러 축이 한꺼번에 사라졌다
- **왜 `grep`으로는 안 되나**: PoB는 「10% increased Archon Buff duration」을
  **부분 파싱한다** — `Duration` INC는 잡고 대상 한정어 `Archon Buff`를 잔여로 남긴다
  (`src/Data/ModCache.lua`). 잔여가 있으면 그 줄을 통째로 버려서 결과가 0이 된다.
  즉 **키워드 유무가 아니라 `parseMod`의 두 번째 반환값**이 판정 기준이다

#### 구현 — 발의안에서 **바뀐 것 3가지**

| 발의 | 실제 | 왜 |
|---|---|---|
| `stats_en`을 우리가 `parseMod`에 통과시킨다 | **PoB가 이미 매긴 플래그를 읽는다**(`node.unknown`/`node.extra`) | 줄 합치기 재시도까지 있는 3단 절차라(`PassiveTree.lua:436-486`) 밖에서 재현하면 그게 **계산 로직 재구현**이다(AD-1). 스냅샷 바뀌면 조용히 어긋난다 |
| `pob_computable: false` 백필 | **`pob_modeling.kind: tree-line-unparsed`** | Passive의 `pob_computable`은 이미 "노드가 PoB 트리에 **없다**"는 뜻(`tree_merge.py:88`)이라 의미가 반대다. 겹치면 `items.py:284`류 후보 배제가 **과잉 작동**한다(#13 재발) |
| 근거는 `PassiveSpec.lua:2607` | **`PassiveTree.lua:487`** | 같은 로직의 사본이 둘 있고, 일반 트리 노드가 실제로 지나는 건 후자다 |

- **실측 (PoB `5d173cb` · 트리 `0_5`)**: 노드 4,912개 중 **501개(10.2%)**에 미파싱 줄.
  줄 단위로 590줄 = `extra` 305 · `unknown` 285. 그중 주얼 소켓 1건(「Sinister Jewel
  Socket」 = 효과가 아니라 **구조 선언**)은 제외해 **500건 표기**. KB 미수록 0건
- **최다 계열**: 「감속 강도 32건」·「유물 수집 거리 14」·「메타 스킬 에너지 14」·
  「영광 생성 13」·**「집정관 지속 10」**·「원소 주입 지속 8」…
- **경고 경로 2층**: 레코드 `pob_modeling`(get_entry) + **`search_kb` 히트의 `pob_gap`
  칸**(인덱스 v8). 후자가 핵심이다 — 레코드 본문에만 두면 **후보를 훑는 단계에서
  안 보이고, 오독은 그 단계에서 굳는다**(#13과 같은 구조)
- **감사 재실행**: `PYTHONPATH=src python -m pok.pob.parse_gaps` (패치 재수집·스냅샷
  교체 후 필수).
  안 돌리면 낡은 판정이 남으므로 **통합 테스트가 정본 표기와 현재 PoB 판정을 대조해
  어긋나면 실패**한다 — 문서 규율이 아니라 강제 지점(철칙 5)
- **부수 확인**: 트리 적용 경로는 잔여를 검사하므로 결과는 **조용한 0**이지
  「조용한 부풀림」이 아니다. 키스톤 「심장정지」는 실측 델타 **0개**로 확인

#### 아이템 편 — ✅ **구현됨** (2026-08-08)

발의 시점의 걱정("KB 원문을 그대로 `parseMod`에 넣으면 값 범위 전처리 차이로 오탐")은
**접근을 바꿔 해소했다** — 트리에서 쓴 수법 그대로, 레코드마다 **진짜 아이템을 세워**
PoB에게 묻는다(`Item.lua`가 `applyRange`로 값 범위를 알아서 푼다).

- **실측**: 레코드 **9,074개(12,565줄)** 검사 → 표기 **1,864건** · 갭 줄 2,257
  (`unknown` 1,650 · `extra` 607)
- **오탐 방어**: 접사는 아무 아이템에나 붙지 않는다(「Recover (8-12) Life when Used」는
  차멤 접사). **한 베이스라도 읽어 내면 갭이 아니다**로 판정 — 베이스 20종을 시도한다.
  ⚠ 실측 결과 **구제 0건**이었다: `parseMod`가 베이스와 무관하기 때문. 그래도 장치는
  둔다(그 전제가 깨지면 판정이 조용히 틀리므로)
- **별개 결함 분리**: 「Socketed Gems are Supported by Level N X」 **7건**은 PoB가
  **예외를 던진다**(PoE2에 없는 서포트를 찾다 죽는다). 조용한 0과 성질이 달라 표기에서
  빼고 따로 센다 → 아래 #17
- **덮지 않는 것**: 룬 슬롯 미매칭(`rune-slot-unmatched`) **3건**은 원인도 대체 방법도
  달라 그대로 둔다
- **검증**: 표기된 접사 3종(집정관 지속·룬 수호·차멤 회복) 델타 **0개**, 대조군
  「+80 최대 생명력」 **47개**. ⚠ 「40% 증가한 화염 피해」도 0개인데 그건 **화염 피해원이
  없어서**다 — 대조군은 더미 빌드에 실제로 효과가 있는 것으로 골라야 한다

### 제안 E — **조립 정본 루프**: 컨셉 확정 후엔 `compute_pob`을 떠난다 (사용자 발의 2026-08-09)

- **발의**: *"compute 스킬은 간단한 계산이나 초기 빌드 선정 과정의 대략적 예측에만 쓰는 게
  좋겠다. 빌드 컨셉 확정 후 본격적으로 만드는 과정에는 적합하지 않다. 지금처럼 pob 데이터를
  기준으로 빌드를 조립하는 과정을 스킬로 만들면 어떨까."*
- **동기**: #27 — 장비를 **세션 메모리에 들고** `compute_pob`으로 20여 회 반복한 결과
  10슬롯 중 4개가 실재하지 않는 장비였다. 파일로 옮겨 `assemble()`에 넘기자 **첫 실행에서**
  전부 드러났다. 문제는 세션의 부주의가 아니라 **정본이 없는 것**이다

**두 단계로 나눈다**

| 단계 | 도구 | 성질 |
|---|---|---|
| 컨셉 탐색 | `compute_pob`·`evaluate_delta`·`optimize_*` | 빠름·개략. 장비 미검사(#27) — **버릴 수치**로 취급 |
| 본조립 | 스펙 파일 → `assemble_pob` → `artifacts/builds/<id>/` | 느림·정본. 적법성 전량 검사, 파일이 진실 |

**루프**: 스펙 파일 1개를 편집 → 재조립 → `validation.json`이 실측·적법성을 남김.
세션이 장비를 기억으로 들지 않으므로 **표류가 구조적으로 불가능**하다

⚠ **전제: #26이 먼저 고쳐져야 한다.** 지금은 어센던시 빌드가 `assemble_pob`를 통과하지
못해 이 루프를 **정식 경로로 돌릴 수 없다**(빌드 세션은 `assemble()`을 직접 불러 우회했다)

**설계 논점 (착수 세션이 정할 것)**
1. **스킬만으로는 부족하다** — 철칙 5. 스킬은 문서고, 문서 규율은 인용까지 하고도 어겨졌다.
   1순위는 #27 요청안 1(`compute_pob` 반환에 적법성 상시 부착)이고, 스킬은 그 위에 얹는
   **절차**다. 순서를 뒤집으면 "스킬에 적어 뒀는데 안 지켰다"가 한 번 더 나온다
2. **스펙 파일의 자리** — `artifacts/`는 gitignore 파생물이라 편집 정본을 두기엔 어색하고,
   scratchpad는 세션과 함께 사라진다. 저장 위치 신설은 **철칙 1(구조 합의)** 사안이다.
   대안: 위치를 만들지 않고 **직전 `build.xml`을 `parse_pob`로 되읽어** 재개한다
3. **기존 `build-generation`과의 경계** — 새 스킬로 뗄지, 그 스킬의 후반 단계(⑧~⑨)로
   넣을지. 컨셉 탐색과 본조립은 **쓰는 도구가 다르므로** 분리가 자연스러워 보이나 판정 사항

---

## 3. 검증으로 뒤집힌 보고 (재발 방지 기록)

**이관 보고 7건이 틀렸다.** 그대로 받았으면 참값을 훼손했다.
**그중 1건은 이 백로그에 적힌 「검증 완료」 자체가 틀린 것이다** — 검증도 검증된다.

| 보고 | 실제 | 무엇이 갈랐나 |
|---|---|---|
| `reservation {min:90, max:39}` 역전 = 파서 버그 | **오기 아님.** 해골 소환수 점유는 레벨이 오를수록 **감소**(PoB `spiritReservationFlat` 90→39). 두 수는 크기 순이 아니라 **레벨 순** | PoB 원본 대조 |
| "쿨다운은 원래 수집돼 있었다 — 조회 도구 결함" | **시점 착시.** 첫 보고 시점엔 정말 없었다(git: `cooldown_s` 이전 1건 → 현재 391건). 엔진이 고친 뒤 조회해서 값이 보인 것 | `git show`로 변경 전 상태 확인 |
| sparse-checkout 로컬 검증 통과 | **Windows CI에서 파일 0개 체크아웃.** git 명령은 성공하고 작업 트리만 빔 | 실제 CI 실행 |
| 〔#14〕 "노드 5571 레코드가 KB에 없다" | **있다.** `passive.the-unseen-path-5571`. 177건이 매칭됐는데 `ORDER BY r.id`가 정작 그 노드를 **164번째**로 밀었을 뿐 | 상위 10건이 아니라 **전량**을 받아 순위를 봤다 |
| 〔#13 검증〕 "unlock_constraint **176건 전부** `ascendancy` 보유, 누락 0" | **179건**이고 **3건은 `ascendancy`가 없다**(선행 노드 요구형). 이전 검증이 Oracle 176건만 세고 나머지를 안 봤다 | 필드 **존재**가 아니라 **값 분포**를 셌다(`Counter`) |
| 〔#24〕 "후보 텍스트 생성기가 explicits를 빠뜨린다" | **아니다.** `render_unique`는 `Onslaught`를 정상 출력한다. 진짜 원인은 **검사기의 12자 문턱**이라 짧은 문구가 비교에서 통째로 빠진 것 | 증상이 아니라 **어느 쪽 코드가 틀렸나**를 직접 실행해 봤다 |
| 〔#18〕 마법사의 피 EHP +51.9% | **빈 스펙에서는 델타 0**이다. 효과가 **장착 플라스크에 걸려** 있어 보고 세션의 문맥(플라스크 3종)에서만 나온다. 결함 자체는 사실 | 같은 아이템을 **다른 문맥**에서 재봤다 |

**그리고 엔진 세션도 틀린다** — 이번엔 내가 방어 축 이름을 `MovementSpeed`로 적었고
(실제는 `MovementSpeedMod`) 후보 20종이 전부 `0.000`으로 찍혔는데도 넘어갔다.
`[빌드]` 세션 이관이 잡았다(#25). **역할 분리가 교차 검증을 만든다**는 §운용의 근거가
검증하는 쪽에도 적용된다.

**교훈**: 지금 보이는 값이 **언제부터 그랬는지**는 별개 질문이다. **로컬 재현이 실제
환경을 대표하지 않을 수 있다.** 그리고 — **0건만 실패가 아니다.** #14는 177건이 왔고
전부 무관해 보였다. 0건 진단(`empty`/`why`)은 0건일 때만 도는데, 이건 **많이 왔는데 다
엉뚱한** 침묵의 실패였다. "결과가 왔다"를 "질의가 성립했다"로 읽지 말 것.

---

## 4. 해결됨 — 무엇을 어떻게 고쳤나 (2026-08-06~09)

측정을 왜곡하던 것들 — 전부 **거부 또는 자동 보고**로 전환(철칙 5 3층).

| 결함 | 조치 | PR |
|---|---|---|
| 〔#33〕 **최적화기가 룬 소켓을 아예 안 봄** — 실측 DPS +69.6%인 축 | `optimize_runes` 신설(실측 그리디 + 규칙). **표기 함정**을 render가 막는다 — 손기입은 증폭이 3배 빠진다 | 대기 |
| 〔#30〕 검사기가 **PoB 스펙 줄을 모드로 오독** — 정상 빌드가 비적법으로 찍힘 | `Sockets:`·`Rune:`·`Radius:`·`Corrupted`를 판정에서 제외. 오탐이 진짜 실격과 섞여 신호를 잃던 것 | 대기 |
| 〔#31〕 **룬 값 5배 부풀림이 LEGAL로 통과** — 룬에만 범위 검사가 없었다 | 상한 = 룬값 * 소켓수 * (1+룬효과)로 대조. **소켓 선언이 없으면 판정 보류** | 대기 |
| 〔#26〕 **어센던시 빌드를 하나도 조립 못 함** (두 검사가 서로를 막음) | 판정식을 「목록 포함」→ **「시작 노드에서 도달 가능」**. `pruned` 사유도 구분 | 대기 |
| 〔#27·#29〕 `compute_pob`이 장비 적법성·요구속성을 안 봄 — **가짜 장비 4개로 20여 회 측정** | 반환에 `items_legal`·`req_shortfall` **상시 부착**. `tree_legal`→`tree_connected` 개명 | 대기 |
| 〔#22〕 **접사 39%가 PoB 미측정인데 조립 결과가 「고점」으로 읽힘** | `optimize_rare`가 KB 표기를 읽어 `unmeasurable`+경고로 낸다(표기는 PR #48이 이미 붙여 뒀다) | #53 |
| 〔#24〕 **Helmet 슬롯이 통째로 죽음** — 도구가 만든 후보를 도구가 거부 | 짧은 explicit을 **줄 단위 일치**로 판정(12자 문턱이 오거부를 낳았다). 유발 레코드는 KB 전체에서 1건 | #52 |
| 〔#18·#25〕 딜 가중이면 방어·이동속도 축이 구조적으로 안 보임 | 방어·유틸 축 **항상 측정** + `defensive_only` 자동 부착. 축 이름 실재는 PoB 실측으로 잠금 | #50·#51 |
| 〔#18〕 **딜 가중이면 순수 방어 유니크가 절대 채택되지 않음** (허리띠 20종 중 12종이 EHP를 올리는데 점수 0) | 방어 축을 가중치와 무관하게 **항상 측정**(비용 0) + `defensive_only`로 자동 부착 + 방어 축 없을 때 경고 | #50·#51 |
| 〔#21〕 중복 id 메시지가 **원본을 범인으로 지목** | 미추적 쪽을 짚고 정본이 어느 쪽인지 명시 | #49 |
| 〔제안 D 아이템편〕 **아이템·룬 접사가 조용히 0으로 측정됨** (9,074개 중 **1,864건**) | 레코드마다 진짜 아이템을 세워 PoB에게 물음. 베이스 20종 시도로 오탐 방어 + 파싱 예외 7건 분리 | #48 |
| 〔#3〕 대리 측정이 **추산인지 실측인지 기록되지 않음** | `ItemSpec.substitutes` 신설 — 선언하면 manifest `substitute_modeling.injected_lines`에 자동 기록. 규약은 build-generation 스킬에 | #48 |
| 〔#16〕 PoB 핀 8곳 분산 | `pok.kb.pob_pin` 하나에서 파생 — **수기 자리 3곳**으로. 4갈래 돌연변이 감지 확인 | #48 |
| 〔#15〕 **키워드 정의가 파일명 알파벳 순서에 따라 버려짐** (493개 중 90개) | 링크 후보 중 **받아올 수 있는 것을 우선**. 정의 255 → **345**, 신규 Mechanic **89건**. 그 안에 #3이 찾던 `Archon_Buff`가 있었다 | #47 |
| 〔#3〕 집정관 지속·회복 **기저 수치 미수록** | poe2db 원문 대조 후 수록 — `base_duration_s: 10` · `recovery_period_s: 20` (유지율 33.3%) | #47 |
| 〔#16〕 PoB 스냅샷 핀 8곳 분산 — 하나 빠뜨리면 증거 체인이 조용히 거짓 | 핀 일치 테스트 + `pob-snapshot` 스킬(교체 절차·감사 재실행 포함) | #47 |
| 〔제안 D / #3〕 **PoB가 못 읽는 트리 문구가 조용히 0으로 측정됨** (노드 4,912개 중 **501개**) | PoB 자체 판정을 덤프해 `pob_modeling` **자동 백필 500건** + `search_kb` 히트에 `pob_gap` 칸(인덱스 v8). 스냅샷 어긋남은 **통합 테스트가 실패**시킴 | #46 |
| 〔#13〕 해금 제약이 **조회 계층에 없음** — 설계 단계에서 오판 | `search_kb(for_ascendancy=)`가 `excluded_by_unlock` 사유를 **자동 부착**(빼지 않고 표시) | #43 |
| 〔#13〕 `optimize_tree` 과잉 배제 — 오라클이 자기 노드를 못 봄 | 스펙 전직을 `candidates()`에 전달 + 표기 해소기(코드↔실명) | #43 |
| 〔#13-b〕 **선행 노드 요구형 해금이 어느 검사에도 없음** | `requires_nodes` — 후보 제외 + **거부** + 검색 표시 | #43 |
| 〔#14〕 이름이 정확히 맞는 레코드가 164위로 밀림 | 질의 시 **이름 일치 우선 정렬** | #43 |
| 다른 어센던시 전용 해금 노드 할당 | KB `unlock_constraint` 수록(179건 백필) + 후보 제외 + **거부** | #35 |
| 어센던시 시작 노드 없이 어센던시 할당 | **거부** | #35 |
| 공급원 없는 config (출혈 강도 x2.76 부풀림) | 요구/공급 구분 후 **거부**. 어휘 밖 축까지 일반화 | #35·#36 |
| 유니크 옵션 조용한 무시 (288.6 vs 433.1) | 이름만 적으면 **거부** + KB explicits 안내 | #38 |
| 클래스·어센던시 조용한 폴백 | **거부** + 정본 코드 안내 | #38 |
| 단계형 스킬 조용한 1단계 (화염파 4.86배) | `stages` 경로 + 미지정 **거부** | #38 |
| 스킬 쿨다운 전량 미수록 | 파서 추가, Skill 401건 **100% 수록** | #37 |
| 룬 수호(Ward) 코스트 미수록 | 자원 목록 추가 | #37 |
| 주얼 접사 풀 11건만 열거 | `jewel` origin 추가 → **171건** | #34 |
| 코스트·점유 min/max 이름이 거짓 | `at_level_1`·`at_max_level` 명시 | #38 |
| CI 34건 실패 (외부 자원 부재) | raw 직접 다운로드 2.9MB + skip 가드 | #36 |
| **#8-b** 몬스터 스킬 문구가 플레이어 `stats`에 혼입 (22건) | 몬스터 카드 분리 + 소환수는 `minion_stats`로 **실체별 수록**, 무관 몬스터는 폐기. **전수 대조 테스트**로 강제 | #44 |
| 젬 문구 재적용이 **멱등하지 않음** — 소스에서 사라진 줄이 눌러붙음 | `gem_stats`가 빈 키를 `None`으로 패치(store 삭제 규약) | #44 |

---

## 5. 참고 — 먼저 읽을 것

- `AGENTS.md` 철칙 4(측정값 ≠ 실현 가능성)·5(규율은 강제 지점이 있어야 한다)
- `search_insights("규율 강제")` → `insight.disciplines-need-enforcement-points`
- `search_insights("PoB 계산기")` → `insight.pob-is-a-calculator-not-a-validator`
- `search_insights("검사 방향")` → `insight.checks-must-look-both-directions`
